# TiranaToDo — stringhe EN

**Mercato: Albania (Tirana) · Lingua: EN · Set caricato: `skill_copy_mercati_eu` (sezione «EN internazionale») + `skill_copy_moderno_2026` + `regole_globali` + `blacklist_formule_ai`**

⛔ Pietre di paragone Keryx **non caricate**: TiranaToDo non è Keryx e non deve suonare come Keryx. Restano attive regole globali, blacklist AI e il gate offerta.

**Lettore:** expat e turisti a Tirana, inglese come lingua franca, livello B1-B2. Frasi corte, parole ad alta frequenza, zero idiomi, zero phrasal verb ambigui, zero riferimenti anglosassoni.
**Nota per SQ e IT:** ogni stringa è costruita con verbi finiti e frasi indipendenti, senza incastri. Si traduce riga per riga senza rompersi. ⛔ `WhatsApp channel` e `[TIRANATODO]` restano in inglese in tutte e tre le lingue.

---

## demo.notice

```
Demo site: the events here are not real. Do not plan your evening on them.
```

`74/90 car` — dice le due cose in due frasi separate (è una demo · gli eventi non esistono) e la seconda dà l'istruzione pratica invece di lasciarla dedurre. «here» invece di «below» perché l'avviso sta anche sulla pagina di invio, dove eventi non se ne vedono. Tolto l'em dash del provvisorio.

---

## follow.title

```
Events we pick, on WhatsApp
```

`27/40 car` — porta la promessa editoriale del §7 del piano («scelti da noi») e dice subito dove arriva la roba. In un blocco che si guarda due secondi la chiarezza batte l'idea furba.

**Le altre due consegnate** (le quattro U su tutte e tre più sotto, in `Titoli scartati`):

- `We do the reading, you go out` — `29/40 car`. Più caldo, dice il beneficio invece del meccanismo. Non nomina il canale: regge solo perché l'icona e il bottone lo dicono già.
- `Get the week in one message` — `27/40 car`. Attacca l'obiezione vera (rumore su WhatsApp) invece del desiderio.

---

## follow.text

```
A few events a week, in one message. No group, no notifications all day.
```

`72/85 car` — prima cosa arriva e ogni quanto, poi l'obiezione numero uno di chiunque riceva un invito WhatsApp. «A few events a week» è volutamente approssimativo: il canale non esiste ancora e una frequenza precisa diventerebbe una promessa da mantenere dal primo giorno.

**Variante da usare finché il canale non è aperto** (vedi `Aperti`):

```
The channel is opening soon. A few events a week, in one message, no group.
```

`75/85 car` — dichiara lo stato invece di far cliccare un link morto.

---

## follow.button

```
Follow on WhatsApp
```

`18/24 car` — dice l'azione e il posto. A 375px sta su una riga sola con margine.

**Le altre due varianti:** `Open the channel` (`16/24`) · `See the channel` (`15/24`). Consegnata la prima perché le altre due non dicono che è WhatsApp, e la sola icona non basta a chi non la riconosce.

---

## footer.follow.title

```
Stay updated
```

`12/20 car` — verbo come le altre due colonne del piede («Browse»). Nessuna parola da decifrare.

---

## footer.follow.link

```
WhatsApp channel
```

`16/20 car` — nomina l'oggetto esatto. «Channel» e non «group» perché la differenza (nessuno può scrivere) è proprio quello che rende accettabile l'iscrizione.

---

## submit.intro

```
There is no form here. You copy the email below, fill in your event and send it. The fields tell you what to write, so you do not have to guess.
```

`144/160 car` — la prima frase risponde alla domanda che il lettore si fa arrivando («dove compilo?»), le altre due tolgono l'attrito vero, che non è la fatica ma il non sapere cosa scrivere. ⛔ Non si dice perché la mail conviene a noi: al lettore non interessa.

---

## submit.step1.title

```
Copy the email template
```

`23/40 car` — verbo all'inizio, oggetto subito dopo. Titoli dei tre passi tutti in imperativo, così si leggono come una sequenza anche saltando i testi.

## submit.step1.text

```
Press the Copy button. The whole message goes to your clipboard, ready to paste.
```

`80/120 car` — nomina il bottone come è etichettato in pagina («Copy»), poi dice cosa succede dopo il clic. «Clipboard» è l'unica parola tecnica e non ha equivalente più comune.

---

## submit.step2.title

```
Fill in your event
```

`18/40 car` — «fill in» è l'unico phrasal verb usato in tutto il set: è quello che ogni non madrelingua incontra sui moduli, quindi non è ambiguo.

## submit.step2.text

```
Write your answer after each field. Field names stay in English. Your text can be in English, Albanian or Italian.
```

`114/120 car` — le due regole che fanno fallire l'invio (nomi dei campi in inglese) e la rassicurazione che serve a un tedesco o a un turco (il testo lo scrivi nella tua lingua). Fonte dei campi: `sop/sop_invio_eventi_via_email.md` §2.2, riga `LANGUAGE`.

---

## submit.step3.title

```
Send it to us
```

`13/40 car` — chiude la sequenza. Nessun avverbio, nessun «now».

## submit.step3.text

```
Send it from the address where you want our reply. Attach a photo if you have one, up to 5 MB.
```

`94/120 car` — il primo dettaglio evita il caso peggiore (mandiamo la risposta a una casella che nessuno legge), il secondo è l'unico numero del set ed è verificato: 5 MB stanno in `PLAN.md` §5.4 e in `sop_invio_eventi_via_email.md` §2.2. La foto è dichiarata facoltativa perché lo è davvero.

---

## submit.mail.subject

```
[TIRANATODO] NEW | category | YYYY-MM-DD | event title
```

`54/60 car` — `[TIRANATODO]` in testa è ciò che lo rende trovabile in una casella piena e ordinabile con un filtro. Il resto è lo schema fisso di `sop_invio_eventi_via_email.md` §2.1: azione, categoria, data ISO, titolo, separatore ` | `.

Esempio compilato, da mostrare sotto al blocco se LORI trova lo spazio: `[TIRANATODO] NEW | music | 2026-09-12 | Jazz Night at Radio Bar`

⚠️ **Ho scritto `NEW`, non `NUOVO`.** La SOP fissa tre valori in italiano (`NUOVO` · `MODIFICA` · `ANNULLA`) su una pagina scritta in inglese per gente che l'italiano non lo parla. Vedi `Aperti`.

---

## submit.button

```
Open your email app
```

`19/30 car` — dice cosa succede al clic, e coincide con l'`aria-label` già in pagina. Un bottone `mailto` che non avvisa apre un'applicazione a sorpresa.

**Le altre due varianti:** `Write the email` (`15/30`) · `Send it by email` (`16/30`). Scartate perché promettono un'azione che il bottone non compie: il messaggio lo scrive e lo manda il lettore, il bottone apre solo il programma.

---

## submit.next

Va dopo il grassetto `What happens next` già presente in pagina.

**Versione A, da usare quando OPS o Davide fissano il tempo:**

```
We read every message and reply within [OPS: tempo di revisione]. If a field is missing we write back and ask for it. If everything is there, the event goes on the site.
```

`169/180 car` con il segnaposto dentro. Sostituendo `[OPS: tempo di revisione]` con `24 hours` scende a `152/180`.

**Versione B, consegnabile oggi senza inventare niente:**

```
We read every message. If a field is missing we write back and ask for it. If everything is there, the event goes on the site.
```

`126/180 car` — nessun tempo dichiarato. Le due condizioni coprono tutti i casi, quindi chi ha mandato sa cosa aspettarsi anche senza una durata.

⚠️ Tutte e due dicono `the event goes on the site`. Oggi il sito è un mockup senza database: `sop_invio_eventi_via_email.md` §1 dichiara che un evento accettato **non ha dove finire online**. Vedi `Aperti`.

---

## ALT delle foto (revisione)

Criteri applicati: descrive la foto e non l'evento · sotto i 150 caratteri · nessuna ambiguità sintattica per chi ascolta con uno screen reader · nessuna parola fuori dalle 3.000 più frequenti.

**Reggono e non si toccano: `cinema` (92 car) e `food` (109 car).** Descrivono la scena, non l'evento, e ogni parola è comune.

### music — corretta

```
An electric guitar and a microphone on a small dark stage, lit by one spotlight, seen over the heads of a seated audience.
```

`122/150 car` — l'originale scriveva «a microphone stand alone in a spotlight»: chi ascolta sente prima *microphone stand* (l'asta) e poi deve tornare indietro, oppure legge *stand alone* come verbo. Due letture possibili nella stessa frase. Sciolto separando l'oggetto dalla luce.

### meetups — corretta

```
People talking in small groups on a rooftop terrace at dusk, with a line of small light bulbs hanging above them.
```

`112/150 car` — «festoon lights» è termine britannico da catalogo: un olandese o un turco non lo decodifica, e su un alt non ha nemmeno il contesto visivo per indovinare. Tolto anche «with drinks in hand», idioma che non aggiunge niente alla descrizione.

### outdoors — corretta

```
A stone path going up through pine trees on a hillside, with the roofs of a city far below in the morning mist.
```

`110/150 car` — «haze» è parola rara per un B2 e non descrive niente che «mist» non descriva. «Climbing» sostituito con «going up» per lo stesso motivo: un sentiero che *climbs* è un uso figurato che i non madrelingua leggono male.

### art — corretta

```
Framed paintings on a white gallery wall, with two visitors standing a few steps away and looking at them.
```

`105/150 car` — «standing back» si legge sia come «indietreggiare» sia come «stare in disparte», e «canvases» è parola da addetti: nella foto si vedono quadri, e quadri si dice `paintings`.

---

## Titoli scartati (restano qui, servono per SQ e IT)

Sedici candidati per `follow.title`, tre consegnati sopra. Le quattro U sui tre consegnati:

| Titolo | Urgente | Unica | Ultra-specifica | Utile |
|---|---|---|---|---|
| `Events we pick, on WhatsApp` | bassa, e per scelta: niente scade | ⛔ non valutabile, concorrenti non letti | media: dice chi sceglie e dove arriva | alta: qualcuno filtra al posto tuo |
| `We do the reading, you go out` | bassa | ⛔ non valutabile | bassa: non nomina il canale | alta: il beneficio è il soggetto |
| `Get the week in one message` | bassa | ⛔ non valutabile | media: una settimana, un messaggio | alta: risponde al rumore su WhatsApp |

⛔ La U «Unica» non si può dichiarare: la lettura dei 3-5 concorrenti del Passo 1-bis punto 5 non è stata fatta. Vedi `Aperti`.

Scartati: `The short list, once a week` · `A few events, straight to your phone` · `We read the whole list, you get five` · `Follow the channel, skip the search` · `One message a week, no group chat` · `The events we would go to` · `Our pick of the week, on WhatsApp` · `You get the five worth going to` · `A short list every week` · `The channel for what is on` · `Picked by us, sent on WhatsApp` · `New in Tirana? Start here` · `Too many events? We choose.`

I due con la domanda cadono per la blacklist (domanda retorica d'apertura). `You get the five worth going to` e `We read the whole list, you get five` cadono per il divieto 4: contengono un numero che oggi sarebbe falso.

---

## Dichiarazione gate

| Gate | Esito |
|---|---|
| **Fatti** | 3 claim classificati. `5 MB` **VERIFICATO** (`PLAN.md` §5.4 + SOP §2.2). Schema oggetto mail **VERIFICATO** (SOP §2.1), con una modifica dichiarata (`NEW` al posto di `NUOVO`). `the event goes on the site` **CORRETTO in stato di rischio**: vero per il sito futuro, falso oggi, segnalato in `Aperti`. ⛔ Zero numeri di pubblico, zero prove sociali, zero tempi di revisione: non esistono e non sono stati inventati. |
| **Struttura** | Non applicabile nella forma standard: il deliverable sono 16 stringhe di interfaccia, non un testo continuo. Nessun blocco piatto di 3+ paragrafi in nessuna stringa. La pagina `submit` esce già vestita: 3 passi numerati, blocco copiabile, un bottone, un box di chiusura. |
| **Readability (EN)** | Stimata a mano, non da tool. Corpo della pagina `submit`: 13 frasi, 117 parole, 9,0 parole per frase, ~1,35 sillabe per parola. **Flesch Reading Ease ≈ 83 (Easy) · Flesch-Kincaid Grade ≈ 4.** Target dichiarato: lettore B1-B2 non madrelingua. Superato con margine. ⚠️ Il GULPEASE non si applica e non compare: il testo è inglese. |
| **Offerta di vendita, 8 check — pagina `submit`** | 1 Promessa **SÌ** (`the event goes on the site`) · 2 Prova **SÌ parziale**: l'unica prova è la dimostrazione, cioè il modello di mail visibile in pagina prima di scrivere. ⛔ Nessuna prova sociale disponibile, e non se ne inventano · 3 Facilità **SÌ** (tre passi, nessun modulo, `so you do not have to guess`) · 4 Velocità **NO**, motivato: il tempo di revisione non esiste ancora, versione A pronta col segnaposto · 5 Probabilità **SÌ** (`If a field is missing we write back and ask for it`: toglie la paura della mail ignorata) · 6 Differenziazione **SÌ** (`There is no form here`, campi fissi) · 7 Offerta **SÌ** al livello del pezzo: gratis, tre passi, nessun account · 8 Ragione per agire ora **NO**, motivato: divieto esplicito del brief, e non c'è niente che scada. |
| **Punteggio pagina `submit`** | **86/120** · quattro P tutti pieni (Problema 12, Promessa 13, Prova 9, Proposta 14) · soglia 75 (non chiede soldi) **superata** · voce più bassa: **Urgente 4/10**, e resta bassa per scelta. |
| **Punteggio blocco `follow`** | **71/120** · quattro P non a zero (Problema 11, Promessa 12, Prova 5, Proposta 13) · soglia 75 **NON superata** · voce più bassa: **Inattaccabile 3/10**, subito dopo **Prova 5/15**. ⛔ Non è sanabile scrivendo meglio: un canale con zero follower non ha prove, e il divieto 4 vieta i numeri. Torna sopra soglia da solo quando ci sarà un track record. Dichiarato, non nascosto. |
| **Lingua italiana (10 voci)** | Non applicabile al deliverable: il copy è in inglese. Passata fatta sulle note italiane di questo file: 10 voci controllate, 0 correzioni. |
| **Blacklist AI** | Passata dedicata, pattern per pattern. **1 match trovato e riscritto**: em dash nell'avviso demo provvisorio. Zero match nel testo nuovo. Verificato anche: zero antitesi «X, not Y», zero tricolon retorici, zero domande retoriche (due candidati scartati per questo), zero aperture da AI, zero superlativi, zero «Discover / Unlock / Join thousands», zero disclaimer difensivi. |
| **Deliverability** | Non applicabile: nessuna email di invio in questo lotto. Il modello di mail è testo che copia il lettore, non un invio nostro. |
| **Correzione Google (gate 1-ter)** | **NON FATTA — in attesa di Davide dal 2026-09-05.** Sopra le 400 battute, quindi il gate si applica. Non lo esegue nessun agente. |
| **Test sul target (gate 3)** | **NON FATTO**: non esiste un profilo `sim_` per expat e turisti a Tirana. Vedi `Aperti`. |
| **STRATEGO (gate 4)** | Non richiesto: nessun prezzo, nessuna offerta, nessun numero di vendita in questo lotto. |

---

## Aperti

**1. Il tempo di revisione, e chi lo dà.**
`submit.next` versione A ha dentro `[OPS: tempo di revisione]`. Il numero esiste già in due posti ma nessuno dei due è ratificato: `PLAN.md` §5 promette «entro 24 ore» per il modulo futuro, e `sop_invio_eventi_via_email.md` §3 lo ripete marcandolo `⏳ STIMA DA VALIDARE` dentro una SOP in stato `PROPOSTA`. Una promessa scritta in pagina è opponibile quanto una in contratto: non la scrivo su una stima.
`DA CHIEDERE A OPS: il «entro 24 ore» di sop_invio_eventi_via_email.md §3 è ratificato o resta stima? Se resta stima, che tempo scriviamo sulla pagina pubblica di invio evento, oppure confermi di non scriverne nessuno?`

**2. `NEW` contro `NUOVO` nell'oggetto della mail.**
La SOP §2.1 fissa tre valori d'azione in italiano su una pagina inglese letta da tedeschi, turchi e greci. Ho scritto `NEW` perché è la stringa che il lettore deve copiare e non deve decifrare, e perché il token serve a una regex, quindi la lingua è arbitraria: va scelta una volta e tenuta. Se OPS conferma l'italiano, cambio la stringa in dieci secondi, ma allora la pagina inglese chiede di scrivere una parola italiana e va spiegato.
`DA CHIEDERE A OPS: i tre valori d'azione dell'oggetto (NUOVO / MODIFICA / ANNULLA, §2.1) passano a NEW / EDIT / CANCEL? Il sito è in inglese e le altre due lingue di interfaccia sono albanese e italiano: il token deve restare uno solo in tutte e tre.`

---
---

# Lotto 3 — due righe di commento in testa al modello copiabile, 2026-09-05

**Mercato: EN internazionale (lingua franca) · Lingua: EN · Set caricato: `skill_copy_mercati_eu` (sezione «EN internazionale») + `skill_copy_moderno_2026` + `regole_globali` + `blacklist_formule_ai`**

**Chi legge:** lo stesso del lotto 2, ma in un momento diverso. Queste due righe **non si leggono sulla pagina**: si leggono dentro la mail già incollata, quando la legenda non è più sotto gli occhi. Sono l'unica istruzione che viaggia col testo.
**Contenitore:** blocco copiabile, **30 caratteri per riga misurati** da LORI a 375 px, ma il vincolo che vale è quello di OPS, proprietario del formato: **massimo 28 caratteri totali per riga, `#` e spazio compresi**. I 2 caratteri di scarto sono margine contro un accento, uno zoom di sistema e l'arrotondamento di un altro browser, e non si spendono. ⚡ Da qui in poi **ogni conteggio in questa sezione è col prefisso**, così è confrontabile col vincolo. Il tetto è secco: una riga `#` che va a capo perde il `#` sulla continuazione e si legge come un campo.
**Perimetro:** dicono due cose sole, e sono le due che la legenda **non** dice. La legenda spiega i formati dei valori; queste spiegano la meccanica del modello.
⛔ **Zero `<` e `>` dentro le due righe**, per il controllo automatico sui segnaposto non sostituiti. Le parentesi si nominano a parole (`brackets`), mai si mostrano.

---

## template.note.1

```
# Replace the brackets too.
```

`27/28 caratteri totali` col prefisso — sotto il tetto di OPS, resta com'è. La parola che lavora è `too`. Che il segnaposto vada sostituito lo capisce chiunque legga `TITLE:` seguito da una parola generica; quello che nessuno fa da solo è togliere le due parentesi, perché sembrano parte del formato, come i due punti dopo il nome del campo. La riga attacca esattamente lì e non spreca caratteri a dire l'ovvio.
`brackets` e non `angle brackets`: tre caratteri in più per una precisione che serve solo a chi conosce già la differenza, e nel modello non c'è nessun altro tipo di parentesi con cui confonderle.
⛔ Scartato `Replace text and brackets.` (28/28 totali, al tetto esatto): `text` è il nome di un segnaposto vero del modello, e la riga si sarebbe letta come una regola sul campo invece che su tutti.

**Opzione B:** `Delete the brackets too.` — `26/28 totali`. Verbo più esplicito sull'atto materiale, ma `delete` da solo può far pensare che si cancellino le parentesi lasciando dentro la parola del segnaposto. Consegnata come seconda perché in traduzione (`Fshini` / `Cancella`) sta più larga.

---

## template.note.2

```
# Keep 19 lines, empty too
```

`26/28 caratteri totali` col prefisso, 2 sotto il tetto — il margine resta al contenitore, non si spende in parole.

⚠️ **Perché è cambiata rispetto alla prima stesura.** La versione precedente era `# Keep all 19 lines, empty too`, contata `28/28` sul **testo**: era il vincolo che avevo ricevuto, non un errore di conteggio. Il vincolo vero di OPS è **28 totali, `#` e spazio compresi**, quindi quella riga stava a **30** e sforava di 2. È caduta la parola `all`, non un fatto.

`all` era l'unico pezzo eliminabile senza perdere niente, perché **il numero fa già il suo lavoro**: `19` è una quantità esatta e non ammette «alcune». Le due parti che restano fanno due lavori diversi e sono intatte. `Keep 19 lines` dà il numero, ed è il numero che rende verificabile l'istruzione: chi ha finito può contare. `empty too` copre il caso che rompe il parsing, cioè la riga cancellata perché non si aveva niente da scriverci. ⛔ Togliere `empty too` era l'altra strada per stare nel tetto, ed è quella scartata: senza, la riga si legge come «riempile tutte» e contraddice la legenda, che dice che 7 campi possono restare vuoti.
⛔ Non è scritto perché l'ordine conta (lettura a macchina): non ci sta, e non serve. L'istruzione è eseguibile senza la ragione.

**Opzione B:** `Keep 19 lines, even empty` — `27/28 totali`. Stessa coppia di fatti. `even empty` è più esplicito sul caso limite, `empty too` è più corto e simmetrico alla riga 1, che chiude anch'essa su `too`: la simmetria aiuta chi legge due righe di seguito in una lingua non sua. Consegnata come seconda.

⚠️ **Non riscrivo il conteggio se LORI misura più largo.** Il tetto lo fissa OPS a 28 totali con la ragione scritta (accenti, zoom, arrotondamento del browser); una misura più generosa nel contenitore non lo sposta.

---

## Dichiarazione gate — lotto 3

| Gate | Esito |
|---|---|
| **Fatti** | 2 claim, entrambi classificati. `19 lines` **VERIFICATO due volte**: `sop/sop_invio_eventi_via_email.md` §2.2, tabella dei 19 campi, stesso conteggio già usato in `legend.intro` del lotto 2 (12 obbligatori + 7 facoltativi = 19); e **confermato dal PM il 05/09/2026**, il modello copiabile in pagina ha 19 righe di campo da `TITLE:` a `CONSENT:`, senza accorpamenti. La condizione aperta nella prima stesura è chiusa: il numero resta. `brackets` **non è un claim**: descrive il formato del documento che il lettore ha davanti. ⛔ Zero numeri nuovi, zero promesse, zero tempi. |
| **Struttura** | Non applicabile nella forma standard: due righe di commento dentro un blocco già disegnato. Nessun blocco piatto. Ogni riga porta un fatto solo e sta su una riga fisica. |
| **Readability (EN)** | Rifatta sul testo definitivo del 05/09. Stimata a mano: 2 frasi, 9 parole, **4,5 parole per frase**, 13 sillabe (`19` contato come si legge, *nine-teen* = 2) → **1,44 sillabe per parola**. **Flesch Reading Ease ≈ 80 (Easy) · Flesch-Kincaid Grade ≈ 3.** ⚠️ Corregge il ≈97 della prima stesura, che contava `19` come una sillaba sola. Target: organizzatore non madrelingua, B1-B2: rientra. Le sei parole portanti (`replace`, `brackets`, `keep`, `lines`, `empty`, `too`) stanno tutte nelle 3.000 più frequenti dell'inglese. ⚠️ GULPEASE non applicabile: testo inglese. |
| **Offerta di vendita, 8 check** | **Non applicabile, e motivato.** Non è un pezzo che vende: è la nota tecnica in testa a un modulo che il lettore ha già deciso di compilare. La decisione di mandare è a monte, sulla pagina, e il punteggio di quella resta il lotto 1 (86/120). Applicare gli otto qui produrrebbe urgenza su un favore. |
| **Punteggio gate offerta** | Non calcolato, per la ragione sopra. Nessuna delle quattro P è toccata. |
| **Lingua italiana (10 voci)** | Non applicabile al deliverable (copy inglese). Passata sulle note italiane di questa sezione: **10 voci controllate, 0 correzioni**. |
| **Blacklist AI** | Passata dedicata rifatta il 05/09 sul testo definitivo, pattern per pattern: **zero match**. Verificato uno per uno: zero em dash, zero punti esclamativi, zero antitesi «X, not Y», zero `simply` / `easily` / `just` / `make sure to`, zero superlativi, zero disclaimer difensivi, zero dichiarazioni di facilità (le due righe **tolgono** un errore, non dicono che è facile). |
| **Controllo angolari** | ⛔ Vincolo bloccante del brief. Rifatto il 05/09 sulle quattro varianti nella forma definitiva, `grep` visivo carattere per carattere (2 prime scelte + 2 opzioni B): **zero `<`, zero `>`**. Aggiunto il conteggio dei caratteri totali col prefisso su tutte e quattro: 27, 26, 26, 27 — **nessuna sopra 28**. Le parentesi sono nominate solo come parola inglese `brackets`. |
| **Deliverability** | Non applicabile: nessuna email inviata da noi. Il testo lo copia e lo manda il lettore dal suo client. |
| **Budget caratteri** | 2 righe su 2 **dentro il tetto**. `template.note.1` **25/28** (3 restituiti). `template.note.2` **28/28** (0 restituiti, al tetto esatto, punto finale tolto per rientrare). ⛔ Nessuno sforo, lista di sforo vuota e dichiarata. |

---

## Aperti (lotto 3)

**1. Le due righe non stanno in 28 caratteri in albanese e in italiano.**
Il margine è zero già in inglese sulla riga 2, e il brief lo dà per noto. Per memoria, con le stesse due coppie di fatti: `Zëvendësoni edhe kllapat` sta a 24 in SQ, ma `Mbani të 19 rreshtat, edhe bosh` è 31 e sfora; in IT `Sostituisci anche le parentesi` è 30 e sfora già la riga 1. ⛔ Non ho tradotto niente: non è in questo lotto. Quando ci si arriva, o il blocco cambia capienza o le due righe diventano tre in SQ e IT, e tre righe di commento sopra `TITLE:` cambiano il peso visivo del blocco.
`DA CHIEDERE A LORI: il blocco copiabile resta a 30 caratteri anche in SQ e IT? Se sì, in quelle due lingue le note di testa non entrano in due righe: preferisci tre righe da 28 o un blocco più largo?`

**2. Le 19 righe del modello non le ho viste.**
Ho scritto `19` sulla fonte SOP §2.2, non su un modello reale: l'estratto del brief si ferma a `...` e poi `CONSENT:`. Se il modello mostrato in pagina accorpa o salta righe, il numero va corretto prima del publish.
`DA CHIEDERE A OPS: il modello copiabile in pagina ha esattamente 19 righe di campo, una per ogni riga della tabella §2.2, o ne accorpa qualcuna? Se il conteggio a schermo non è 19, la nota di testa dice un numero falso.`

**3. `the event goes on the site` è una promessa che oggi non si mantiene.**
`sop_invio_eventi_via_email.md` §1 dichiara che il mockup non ha database e che un evento accettato oggi non ha dove finire online. Finché è così, questa stringa non deve andare in produzione su un indirizzo pubblico: chi manda un evento aspetta di vederlo e non lo vedrà mai. Non è un problema di copy, quindi non l'ho aggirato riscrivendolo: se serve una versione «riceviamo ma non pubblichiamo ancora», la scrivo, ma prima qualcuno deve decidere se la pagina di invio si pubblica ora o alla Fase 2 di `PLAN.md`.
`DA CHIEDERE A DAVIDE: la pagina Submit an event va online prima che esista il sito con il database? Se sì servono stringhe diverse, perché oggi non possiamo pubblicare un evento che riceviamo.`

**4. L'URL del canale WhatsApp non esiste.**
`PLAN.md` §9-bis: il canale lo crea Davide. In pagina `data-ph="whatsapp-channel-url"` punta a `#`. Ho consegnato per questo una variante di `follow.text` che dichiara «The channel is opening soon». ⛔ Non ho scritto da nessuna parte che il canale è già attivo o seguito.

**5. La U «Unica» non è dichiarabile.**
Il Passo 1-bis punto 5 della SOP copy vuole 3-5 concorrenti letti prima di scrivere, e li raccoglie RADAR. Non è stato fatto, quindi sui tre titoli consegnati «Unica» resta `non valutabile`. Concorrenti veri di TiranaToDo su Tirana: gruppi Facebook di expat, canali Instagram di locali, `visittirana.al` e simili.
`DA CHIEDERE A RADAR: quattro righe per ognuno dei 3-5 posti dove oggi un expat a Tirana cerca cosa fare la sera (gruppi Facebook, canali Instagram, siti eventi locali): promessa testuale, registro, le tre parole che tornano su tutti, cosa nessuno dice.`

**6. Manca un profilo target simulato.**
Il gate 3 della SOP copy vuole una lettura su profilo `sim_`. Per l'expat a Tirana non esiste. Senza, il linguaggio di queste stringhe resta **un'ipotesi dedotta, non raccolta**: nessun verbatim reale di expat a Tirana è stato letto prima di scrivere.
`DA CHIEDERE A CHRISTOPHER: serve un profilo per l'expat arrivato a Tirana da poche settimane, non madrelingua inglese, che cerca cosa fare la sera. Tre pain con le sue parole, uno stato desiderato a sei mesi, un registro emotivo.`

**7. Non consegnato perché non richiesto.**
Restano segnaposto e non li ho toccati: il titolo `Submit an event` (H1 e navigazione, già in uso), le etichette delle undici categorie, i chip dei filtri, le voci del piede `About` / `Contact` / `Privacy`, l'elenco dei campi del modello mail (`[OPS: elenco campi]`), i titoli e le descrizioni dei diciassette eventi finti di `docs/eventi.js`. Quest'ultima è la parte più grossa che manca: se il mockup si mostra a qualcuno, quei testi sono i primi che si leggono.

---
---

# Lotto 2 — legenda dei campi (`.te-legend`), 2026-09-05

**Mercato: EN internazionale (lingua franca) · Lingua: EN · Set caricato: `skill_copy_mercati_eu` (sezione «EN internazionale») + `skill_copy_moderno_2026` + `regole_globali` + `blacklist_formule_ai`**

**Chi legge:** chi organizza un evento — bar, centro culturale, chi organizza incontri per expat. Non compra niente: ci sta facendo un favore. Registro identico al lotto 1: frasi corte, verbi finiti, zero idiomi, zero entusiasmo, zero vendita.
**Contenitore:** blocco `.te-legend` dopo il modello copiabile. I valori accettati (`YYYY-MM-DD`, `music`, `YES`, `NO`, `500 ALL`, `EN`…) sono **chip a schermo**, e i nomi dei campi sono **etichette a schermo**: nessuna stringa li ripete, salvo quando la regola riguarda un campo diverso da quello della riga (`ALL_DAY` dentro `rule.times`, `IS_FREE` dentro `rule.price`, `CONSENT` dentro `rule.yesno`).
**Fonte dei fatti:** `sop/sop_invio_eventi_via_email.md` §2.2, tabella dei 19 campi. Nessun numero fuori da lì.
**Budget caratteri — sciolto da Davide il 05/09/2026:** i tetti di questa sezione (60 e 70 caratteri) si misurano **sul blocco copiabile**, non sulla legenda. ⚡ La legenda è in font proporzionale e a parità di caratteri occupa meno spazio: il tetto **non si allarga per questo**. ⛔ Il conteggio non si rifà sul contenitore della legenda.


---

## legend.title

```
How to fill the fields
```

`22/30 car` — usa lo stesso verbo del passo 2 già in pagina (`Fill in your event`), così il lettore capisce che questo blocco è la continuazione di quell'istruzione e non un'altra cosa. Scartati `Field rules` (troppo secco, sembra un elenco di divieti) e `What goes in each field` (23 car, ma «goes in» è un phrasal verb evitabile).

---

## legend.intro

```
12 of the 19 fields are required. The rules below cover the formats.
```

`68/80 car` — il numero arriva prima di tutto perché è la sola domanda che si fa chi guarda un modello di 19 righe («devo riempirle tutte?»). Seconda frase senza pronomi: «the formats» ha un referente visibile, «them» avrebbe potuto significare sia i 12 obbligatori sia tutti e 19. Fonte del 12/19: SOP §2.2, colonna Obbligatorio (12 sì, 7 no).

---

## legend.optional.lead

```
Optional fields
```

`15/20 car` — attacco bold del riquadro. Nomina la categoria invece di dare un'istruzione, così regge sia se i sette nomi sono elencati sotto sia se sono chip di fianco.

## legend.optional.text

```
These 7 can stay empty. The other 12 all need an answer.
```

`56/60 car` — dice la stessa cosa da due lati opposti: cosa puoi saltare e cosa no. La seconda frase esiste perché il rischio vero non è lasciare vuoto un facoltativo, è lasciare vuoto un obbligatorio credendo che valga la stessa libertà. «all» prima di «need» chiude la scappatoia del «quasi tutti».

⚠️ **Assunzione dichiarata:** `These 7` ha un referente solo se i sette nomi dei campi facoltativi sono visibili dentro il riquadro (elenco o chip). Vedi `Aperti (lotto 2)` punto 1.

---

## legend.freetext

```
Fields not listed here are free text. Write them the way you want.
```

`66/95 car` — è la stringa che toglie il lavoro inutile: senza, l'organizzatore cerca una regola anche per `TITLE`, `VENUE_NAME`, `ADDRESS`, `ORGANIZER_NAME`, e non trovandola si ferma o scrive una mail per chiedere. «Free text» è la stessa espressione della SOP §2.2 e non ha equivalente più comune.

---

## rule.category

```
Pick one. Write it in lower case, exactly as shown.
```

`51/60 car` — tre regole in nove parole: una sola, minuscolo, copiata identica. «exactly as shown» rimanda ai chip senza ripeterne il contenuto. Il controllo §4.3 della SOP dice che una categoria non riconosciuta fa partire una mail di chiarimento: questa riga è quella che la evita.

---

## rule.dates

```
Year, then month, then day. Two digits for month and day.
```

`57/60 car` — il chip mostra la forma, la frase spiega l'ordine, che è ciò che un americano e un albanese leggono in modo diverso (`12/09`). La seconda frase copre l'errore più frequente dopo l'ordine: `2026-9-5` invece di `2026-09-05`, che rompe il parsing futuro previsto dalla SOP §2.1.

---

## rule.times

```
Use the 24 hour clock. You can skip the start time only if ALL_DAY is YES.
```

`74/85 car` — la condizionale è scritta con `only if`, non con `unless` e non con `except when`: `only if` ha una lettura sola, dichiara che quello è **l'unico** caso in cui si può lasciare vuoto. `unless` viene letto da molti non madrelingua come un semplice «se non», e apre la porta a «l'evento è di sera, quindi non è tutto il giorno, quindi posso saltarla». Fonte: SOP §2.2 riga 4.
`ALL_DAY` è nominato per esteso perché è un campo **diverso** da quello della riga: senza il nome, la condizione non sarebbe agganciabile.

---

## rule.yesno

```
Only YES or NO. CONSENT must be YES, or we cannot use your event.
```

`65/70 car` — «Only» è la parola che lavora: i chip mostrano i due valori ammessi ma non vietano `true`, `Yes`, `po`, `sì`, `free`. La seconda frase dice la conseguenza invece della regola, perché una regola senza conseguenza si legge come una preferenza. ⛔ Non è una minaccia e non è un tempo: dice cosa non possiamo fare, non cosa succederà a lui. Fonte: SOP §2.2 riga 19 e §4, motivo di rifiuto 7.

---

## rule.price

```
Needed only when IS_FREE is NO. Leave it empty for free events. Always in Lek.
```

`78/85 car` — la stringa copre tutte e tre le facce della regola, ed è voluto: la condizione (`only when … is NO`), il caso opposto scritto per esteso (`leave it empty`), la valuta. Il controllo §4.5 della SOP verifica proprio la coppia `IS_FREE`/`PRICE` nei due sensi, quindi scrivere solo metà regola sposterebbe l'errore invece di toglierlo.
`Always in Lek` sostituisce la formulazione al negativo «no other currency»: dice la stessa cosa e lascia 7 caratteri di margine.

---

## rule.photo

```
Pick one of the three. An attached file can be JPG, PNG or WebP, up to 5 MB. NONE is fine.
```

`89/110 car` — `Pick one of the three` dice che i tre valori si escludono, cosa che i chip affiancati non dicono. I limiti stanno solo sulla frase dell'allegato, così chi manda un link non li applica per sbaglio a sé. `NONE is fine` è la traduzione della riga di SOP §2.2 «l'evento non si ferma per l'immagine»: senza, chi non ha una foto pensa di non poter mandare l'evento, ed è il caso peggiore per noi.
`5 MB` **verificato**: SOP §2.2 (foto) e `PLAN.md` §5.4. È lo stesso numero già scritto in `submit.step3.text` del lotto 1, quindi la pagina non dichiara due limiti.

---

## rule.description

```
Up to 1500 characters. Shorter is fine.
```

`39/70 car` — scritto `1500` senza separatore di migliaia: `1,500` si legge «uno virgola cinque» per un tedesco, un olandese e un italiano, che sono tre delle quattro nazionalità del pubblico. La seconda frase toglie la lettura «serve una descrizione lunga», che il solo tetto suggerisce. Fonte: SOP §2.2 riga 13.

---

## rule.language

```
The language of your text. If you leave it empty, we use EN.
```

`60/70 car` — la prima frase dice **di cosa** parla il campo, perché è l'unico dei 19 dove il nome non basta (lingua del testo dell'evento, non lingua in cui vuoi la risposta). La seconda dichiara il comportamento senza chiedere niente. Fonte: SOP §2.2 riga 18.

---

## Dichiarazione gate — lotto 2

| Gate | Esito |
|---|---|
| **Fatti** | 6 claim, tutti classificati. `12 of the 19 fields are required` **VERIFICATO** (SOP §2.2, conteggio colonna Obbligatorio: 12 sì, 7 no). `up to 5 MB` **VERIFICATO** (SOP §2.2 + `PLAN.md` §5.4). `JPG, PNG or WebP` **VERIFICATO** (SOP §2.2). `1500 characters` **VERIFICATO** (SOP §2.2 riga 13 + `PLAN.md` §5.3). `assente = EN` **VERIFICATO** (SOP §2.2 riga 18 + `PLAN.md` §6). `CONSENT` bloccante **VERIFICATO** (SOP §2.2 riga 19 + §4 motivo 7). ⛔ Zero tempi di risposta dichiarati, zero promesse di pubblicazione: la legenda descrive un formato. |
| **Struttura** | Non applicabile nella forma standard: 13 stringhe di interfaccia dentro un contenitore già disegnato da LORI. Nessun blocco piatto: ogni regola è una o due frasi, agganciata all'etichetta del suo campo. |
| **Readability (EN)** | Stimata a mano, non da tool. Le 13 stringhe: 24 frasi, 158 parole, **6,6 parole per frase**, ~1,28 sillabe per parola. **Flesch Reading Ease ≈ 88 (Easy) · Flesch-Kincaid Grade ≈ 3.** Target: organizzatore non madrelingua, inglese B1-B2. Superato con margine, ed è più facile del lotto 1 (83) perché qui si danno istruzioni, non si spiega un servizio. ⚠️ GULPEASE non applicabile: testo inglese. |
| **Offerta di vendita, 8 check** | **Non applicabile, e motivato.** Non è un pezzo che vende: nessun prezzo, nessuna offerta, nessuna azione da comprare. Il brief di lancio vieta esplicitamente vendita, entusiasmo e ragione-per-agire-ora. Applicare gli otto qui produrrebbe il danno che i check esistono per evitare: urgenza fabbricata su un favore che qualcuno ci sta facendo. Il gate 8 resta valido sulla pagina `submit` nel suo insieme, dichiarato nel lotto 1 (86/120). |
| **Punteggio gate offerta** | Non calcolato, per la ragione sopra. Il punteggio della pagina resta quello del lotto 1: **86/120, soglia 75 superata**. Le 13 stringhe nuove non toccano nessuna delle quattro P: aggiungono istruzioni operative sotto il modello, dopo la decisione di mandare. |
| **Lingua italiana (10 voci)** | Non applicabile al deliverable (copy inglese). Passata sulle note italiane di questa sezione: **10 voci controllate, 0 correzioni**. |
| **Blacklist AI** | Passata dedicata, pattern per pattern: **zero match**. Verificato uno per uno: zero em dash (i due trattini presenti sono in `sop_invio_eventi_via_email.md` citato, non nelle stringhe), zero antitesi «X, not Y», zero domande retoriche, zero tricolon retorici (`JPG, PNG or WebP` è un elenco di fatti), zero aperture da AI, zero superlativi, zero «simply / easily / just», zero «make sure to», zero disclaimer difensivi. ⚠️ Controllato anche l'attrito dichiarato invece che ridotto (regola 3 del gate): nessuna stringa dice che è facile, `Shorter is fine` e `NONE is fine` **tolgono** lavoro invece di dichiararlo leggero. |
| **Deliverability** | Non applicabile: nessuna email inviata da noi. Il modello è testo che copia il lettore. |
| **Budget caratteri** | 13 stringhe su 13 **dentro il tetto**. Nessuno sforo. Margine totale: 875 caratteri assegnati, **690 usati, 185 restituiti** (21%). Riga più stretta: `legend.optional.text` (56/60). Riga più larga di margine: `rule.description` (39/70). |

---

## Aperti (lotto 2)

**1. `These 7` ha bisogno dei sette nomi visibili.**
`legend.optional.text` funziona solo se dentro il riquadro dei facoltativi si vedono i sette nomi di campo (`DATE_END TIME_END TICKET_URL DESCRIPTION PHOTO ORGANIZER_PHONE LANGUAGE`), elencati o come chip. Se il riquadro contiene solo lead e testo, «These 7» non ha referente e la stringa va riscritta con i nomi dentro, che però non stanno in 60 caratteri.
`DA CHIEDERE A LORI: nel riquadro .te-legend dei campi facoltativi i sette nomi sono a schermo (chip o elenco) accanto a legend.optional.lead? Se no, serve un budget diverso per legend.optional.text, perché i sette nomi da soli sono 78 caratteri.`

**2. Le versioni SQ e IT non sono in questo lotto.**
Il sito ha tre lingue di interfaccia (`PLAN.md` §6). Queste 13 stringhe sono solo EN. Sono scritte con verbi finiti e frasi indipendenti, quindi si traducono riga per riga senza rompersi, ⛔ ma **i budget caratteri non reggono in traduzione**: albanese e italiano crescono di circa il 15-20% su frasi corte come queste, e `rule.dates` (57/60) e `legend.optional.text` (56/60) sforerebbero di sicuro.
`DA CHIEDERE A LORI: il blocco .te-legend ha la stessa capienza in tutte e tre le lingue? Se sì servono budget EN più stretti, perché SQ e IT non entrano negli stessi caratteri.`

**3. Resta aperto dal lotto 1 e tocca anche questo blocco.**
`NEW` contro `NUOVO` nell'oggetto (Aperti lotto 1, punto 2) non è stato deciso. Non tocca nessuna delle 13 stringhe di legenda, che non nominano l'azione, ⛔ ma se OPS conferma l'italiano la pagina inglese chiede di scrivere una parola italiana e serve una quattordicesima riga di legenda per l'oggetto. Non l'ho scritta: dipende da una decisione che non è mia.

---
---

# Lotto 4 — questionario a passi, pagina Submit an event, 2026-09-05

**Mercato: EN internazionale (lingua franca) · Lingua: EN · Set caricato: `skill_copy_mercati_eu` (sezione «EN internazionale») + `skill_copy_moderno_2026` + `regole_globali` + `blacklist_formule_ai`**

⚡ **Questo lotto sostituisce l'esperienza dei lotti 1, 2 e 3, non li cancella.** Ordine di Davide del 05/09/2026: il blocco monospazio da copiare a mano e la legenda di 8 regole spariscono dal percorso principale. L'organizzatore risponde a domande normali e alla fine un bottone apre la sua mail già scritta. ⚠️ Il blocco copiabile **resta vivo nella via di riserva** (§6b): chi non ha un client di posta lo copia. Quindi `template.note.1` e `template.note.2` del lotto 3 non si buttano.

**Chi legge:** un organizzatore di eventi a Tirana — un locale, un collettivo, un'associazione, una persona che fa un concerto. Spesso albanese, inglese seconda lingua, B1-B2. ⛔ Non compra niente: ci sta facendo un favore.
**Registro:** pratico, gentile, zero promozione, zero punti esclamativi, zero idiomi, zero phrasal verb ambigui. Verbi finiti, una frase un fatto.
**Budget:** tetti del PM del 05/09/2026, misurati sul testo dell'interfaccia. ⛔ Il tetto storico di 60 caratteri dei lotti 1-2 **non si applica qui**: era misurato sul blocco monospazio, che esce dal percorso principale.
**Forma delle etichette, scelta e dichiarata:** etichetta nominale breve per i campi che chiedono un dato (`Start time`), etichetta interrogativa per i tre campi sì/no (`Is it free?`). Un sì/no senza domanda costringe il lettore a ricostruirla da solo, ed è l'unico punto dove la domanda vale i caratteri.

---

## 1 · Titolo e sottotitolo

### submit.title

```
Send us your event
```

`18/45 car` — verbo, destinatario, oggetto. `Send` e non `Submit`: `submit` è parola da modulo istituzionale e per un B1 sta vicino a «sottomettersi». Nessuna delle tre parole va cercata sul dizionario.

**Le altre due varianti:** `Add your event` (`14/45`) · `Tell us about your event` (`24/45`). La prima descrive quello che succede nel database, cioè un fatto nostro, non suo. La seconda è più calda ma promette una conversazione e qui c'è un questionario.

### submit.subtitle

```
Answer a few questions. At the end your email opens, already written.
```

`69/110 car` — dice le due cose che tolgono la paura del modulo lungo: sono domande, e la fatica finale non c'è. `already written` è il punto: chi arriva su una pagina di invio si aspetta di dover comporre un messaggio, e sapere che non deve lo fa partire.
⚡ 41 caratteri restituiti, ed è voluto: è la stringa che cresce di più in traduzione (SQ e IT girano intorno a `Përgjigjuni disa pyetjeve` / `Rispondi a qualche domanda`, che sono più lunghe dell'inglese di 3-6 caratteri già in apertura).

⛔ Scartato `No form to fill in. Just answer and press send.` (`46/110`): `just` è in blacklist, e la frase dichiara che è facile invece di renderlo facile. La facilità la fa il questionario, non l'aggettivo.

---

## 2 · I 19 campi — etichetta e aiuto

**Aiuto vuoto su 2 campi su 19, e sono dichiarati qui:** `DATE_START` e `IS_FREE`. L'etichetta dice già tutto e un aiuto avrebbe aggiunto una riga da leggere senza aggiungere un fatto. ⛔ Non si scrive un aiuto per riempire una casella di layout.

Sui 17 restanti l'aiuto esiste perché porta **una** delle quattro cose: un formato che si sbaglia (`TIME_START`), una condizione (`DATE_END`), un permesso a lasciare vuoto (`TIME_END`), o una promessa su cosa diventa pubblico (i tre `ORGANIZER_*`).

| # | Campo | `field.N.label` | car | `field.N.help` | car |
|---|---|---|---|---|---|
| 1 | TITLE | `Event name` | `10/35` | `Example: Open mic night` | `23/85` |
| 2 | CATEGORY | `Category` | `8/35` | `Pick one. If nothing fits, pick Other.` | `38/85` |
| 3 | DATE_START | `Start date` | `10/35` | *(vuoto)* | — |
| 4 | TIME_START | `Start time` | `10/35` | `Use the 24 hour clock, like 21:00. Skip it for an all day event.` | `64/85` |
| 5 | DATE_END | `End date` | `8/35` | `Only if the event ends on a different day.` | `42/85` |
| 6 | TIME_END | `End time` | `8/35` | `You can leave this empty if you do not know it.` | `47/85` |
| 7 | ALL_DAY | `Does it last all day?` | `21/35` | `Yes for markets, fairs and festivals with no fixed start time.` | `62/85` |
| 8 | VENUE_NAME | `Place name` | `10/35` | `The name of the bar, club or space where it happens.` | `52/85` |
| 9 | ADDRESS | `Address in Tirana` | `17/35` | `Street and area, so people can find it. Example: Rruga Sami Frashëri, Blloku` | `76/85` |
| 10 | IS_FREE | `Is it free?` | `11/35` | *(vuoto)* | — |
| 11 | PRICE | `Ticket price in Lek` | `19/35` | `Only the number in Lek, like 500.` | `33/85` |
| 12 | TICKET_URL | `Ticket link` | `11/35` | `If tickets are sold online, paste the full link here.` | `53/85` |
| 13 | DESCRIPTION | `What happens` | `12/35` | `Optional, but the page looks empty without it. Up to 1500 characters.` | `69/85` |
| 14 | PHOTO | `Photo` | `5/35` | `Attach a file, paste a link, or choose none. JPG, PNG or WebP, up to 5 MB.` | `74/85` |
| 15 | ORGANIZER_NAME | `Your name or your group` | `23/35` | `We show this name with the event.` | `33/85` |
| 16 | ORGANIZER_EMAIL | `Your email` | `10/35` | `We use it to answer you. We never show it on the site.` | `54/85` |
| 17 | ORGANIZER_PHONE | `Your phone` | `10/35` | `Only if we need to ask something fast. It stays private.` | `56/85` |
| 18 | LANGUAGE | `Language of the event` | `21/35` | `The language spoken at the event. Empty means English.` | `54/85` |
| 19 | CONSENT | `Can we publish it?` | `18/35` | `We need a yes. Without it we cannot publish your event.` | `55/85` |

**Le scelte che non si vedono dalla tabella:**

- **1 TITLE.** L'aiuto è solo un esempio perché il difetto vero non è il formato, è la genericità: senza esempio si riceve `Concert`. L'esempio non nomina un locale reale, così non si legge come un evento vero pubblicato da noi.
- **4 TIME_START.** Due fatti in due frasi separate: l'orologio a 24 ore (l'errore di formato) e il permesso di saltarlo (la condizione della SOP §2.2 riga 4). ⛔ Non ho usato `unless`: molti non madrelingua lo leggono come un «se non» morbido e si autorizzano a lasciare vuoto un evento serale.
- **7 ALL_DAY.** L'aiuto dà tre esempi concreti invece di spiegare il concetto, perché «tutto il giorno» sembra ovvio e non lo è: chi fa un concerto dalle 21 alle 2 pensa che duri tutta la notte, quindi tutto il giorno.
- **9 ADDRESS.** `Street and area` chiede due cose, e l'area è quella che rende trovabile un indirizzo a Tirana. L'esempio è preso da `docs/eventi.js` (`Rruga Sami Frashëri, Blloku`) e non è un claim: è la forma di un indirizzo, non l'affermazione che lì ci sia un evento.
- **11 PRICE.** `Only the number in Lek` toglie in cinque parole i tre errori che la SOP §4.5 deve poi rincorrere: la valuta scritta a mano, il simbolo, e la parola `free` dentro un campo numerico.
- **13 DESCRIPTION.** È l'unico campo dove serviva dire «facoltativo ma vale la pena». `the page looks empty without it` dice una conseguenza materiale invece di raccomandare. ⛔ Scartato `People decide from this`: è un'affermazione sul comportamento del pubblico e non ho niente che la provi.
- **15-16-17, ed è il punto che il brief chiama unico.** Le tre righe formano una promessa completa e si leggono di seguito: **uno si mostra, uno serve a risponderti, uno resta privato.** Il verbo cambia apposta a ogni riga (`we show` · `we never show` · `it stays private`) perché tre frasi con lo stesso verbo si leggono come una formula legale e si saltano. ⛔ Nessuna delle tre dice «i tuoi dati sono al sicuro»: è la formula che fa sospettare, e non la manterremmo perché non c'è una privacy policy pubblicata a reggerla.
- **19 CONSENT.** Dice cosa **non possiamo fare noi**, non cosa succede a lui. Una regola senza conseguenza si legge come una preferenza e si salta.

---

## 3 · Le 12 categorie

**Le undici già in `docs/eventi.js` (`window.TE_CAT_LABEL`) le confermo tutte.** Sono parole singole ad alta frequenza, nessuna richiede un dizionario, e cambiarle costerebbe un allineamento su tre file per zero guadagno.

| chiave | etichetta | car | nota |
|---|---|---|---|
| `music` | `Music` | `5/35` | confermata |
| `nightlife` | `Nightlife` | `9/35` | confermata |
| `theatre` | `Theatre` | `7/35` | confermata. Grafia britannica, coerente col resto del sito e standard in Europa |
| `cinema` | `Cinema` | `6/35` | confermata |
| `art` | `Art` | `3/35` | confermata |
| `food` | `Food & Drink` | `12/35` | confermata. ⚠️ In `eventi.js` sta come `Food &amp; Drink`: è HTML escaped, corretto lì, ⛔ ma se il questionario la stampa come testo l'entità si vede a schermo |
| `sport` | `Sport` | `5/35` | confermata. Singolare, uso britannico ed europeo |
| `family` | `Family` | `6/35` | confermata |
| `meetups` | `Meetups` | `7/35` | confermata |
| `outdoors` | `Outdoors` | `8/35` | confermata |
| `workshop` | `Workshops` | `9/35` | **nuova** |
| `other` | `Other` | `5/35` | confermata |

**`Workshops` al plurale, e la ragione è che il singolare mente.** Le altre undici etichette nominano un ambito (`Music`, `Sport`), e un ambito sta bene al singolare. `Workshop` da solo nomina invece **un** evento, quindi in una lista di categorie si legge come «questo evento è un workshop» e non come «la categoria dei workshop». `Meetups` e `Outdoors` sono già plurali per lo stesso motivo. ⚠️ La chiave resta `workshop` singolare, come da ordine di Davide.

⛔ Scartato `Courses & workshops`: è più chiaro ma è la sola etichetta con due parole e una `&`, e in un elenco di chip a 375 px una riga più lunga delle altre si nota più di quanto aiuti.

---

## 4 · Bottoni e navigazione

| stringa | testo | car |
|---|---|---|
| `nav.next` | `Next` | `4/22` |
| `nav.back` | `Back` | `4/22` |
| `submit.open` | `Open my email` | `13/22` |
| `submit.copy` | `Copy the text` | `13/22` |

- **`Next` / `Back`.** Due parole del vocabolario base, simmetriche, uguali su ogni passo. ⛔ Scartati `Continue` (8/22) e `Previous` (8/22): entrambe corrette, ⚠️ ma in albanese e in italiano `Previous` diventa `E mëparshme` e `Precedente`, che sono i due bottoni più lunghi di tutto il set. `Back` regge la traduzione (`Prapa` / `Indietro`).
- **`Open my email`.** Prima persona apposta: è il **suo** programma di posta che si apre, e il possessivo è quello che gli dice che non stiamo mandando niente al posto suo. Varianti: `Open the email` (`14/22`) · `Open your email app` (`19/22`, coincide con l'`aria-label` già in pagina). ⛔ Scartato `Send my event` (`13/22`): il bottone non manda niente, e un bottone che promette un invio e apre un'applicazione fa chiudere la pagina.
- **`Copy the text`.** Nomina l'oggetto invece del solo verbo, perché nella via di riserva il lettore ha davanti due cose (il testo e un indirizzo) e `Copy` da solo non dice quale. Varianti: `Copy the email` (`14/22`) · `Copy` (`4/22`).

⚠️ **Non consegnato perché non richiesto e non lo invento:** l'indicatore di passo (`Step 4 of 19` o simile). Se il questionario lo mostra, la stringa serve e ha un vincolo suo. Vedi `Aperti (lotto 4)` punto 4.

---

## 5 · Errori su campo obbligatorio vuoto

Sei stringhe, una per tipo. Tutte cominciano con `Please`: è la parola che tiene il tono gentile del brief senza costare una frase, ed è la prima che un B1 riconosce come «non è colpa tua, manca una cosa». ⛔ Nessuna dice cosa è andato storto: dice cosa fare adesso.

| tipo | testo | car |
|---|---|---|
| testo | `Please write something here.` | `28/60` |
| data | `Please pick a date.` | `19/60` |
| ora | `Please pick a time.` | `19/60` |
| scelta | `Please pick one of the options.` | `31/60` |
| email | `Please write a valid email, like name@mail.com.` | `47/60` |
| numero | `Please write the price as a number, like 500.` | `45/60` |

**Settima stringa, fuori elenco e serve.** `TIME_START` è l'unico obbligatorio **condizionato** dei 19 (SOP §2.2 riga 4): se il lettore lo lascia vuoto perché il suo è un evento di un giorno intero, l'errore generico gli dice una bugia e lo blocca su un passo che può saltare.

```
Please pick a time, or say it lasts all day.
```

`44/60 car` — dà le due uscite, e la seconda è la risposta a `ALL_DAY`.

⚠️ **L'errore su email e numero non è un errore di campo vuoto.** Quei due campi possono essere pieni e sbagliati, e le due stringhe qui sopra coprono tutti e due i casi con lo stesso testo, che è la scelta giusta per un questionario a passi: chi legge non deve distinguere «vuoto» da «malscritto», deve solo sistemare la riga.

---

## 6 · Il momento finale

### 6a · `submit.ready` — la frase accanto al bottone

```
Your answers are ready. The button opens your email app with the message written. You only press send.
```

`102/140 car` — tre frasi, tre fatti, in ordine di cosa il lettore vuole sapere: **è finito · cosa fa il bottone · cosa resta da fare a te.**
La seconda frase esiste per una ragione sola: un bottone che apre un'applicazione senza avvisare fa chiudere la pagina, ed è il punto in cui si perde un evento già scritto per intero. La terza è la sola parte che chiede qualcosa, ed è quattro parole.
⛔ Non c'è nessun `almost there`, nessun `great`, nessun ringraziamento anticipato: sta ancora facendo il lavoro, e congratularsi prima della fine è la formula che rende evidente che dall'altra parte non c'è nessuno.

**Variante B:** `Everything is ready. The button opens your email app with the message already written inside.` (`93/140`). Dice meno: non nomina l'ultima azione, e senza `You only press send` una parte dei lettori chiude il client di posta pensando di aver finito.

### 6b · `submit.fallback` — la via di riserva

```
If nothing opened, there is no email app set up here. Copy the text below and send it to events@tiranatodo.com
```

`110/140 car` — due frasi, e la prima è la diagnosi che il lettore non sa fare da solo. Chi preme un `mailto` e vede la pagina ferma pensa che il sito sia rotto, non che manchi un programma sul suo telefono: senza la prima frase la seconda sembra una scusa.
`set up here` e non `on your phone`: la pagina si apre anche da un computer, e una diagnosi sbagliata vale meno di nessuna diagnosi.
⛔ Niente domanda in apertura (`Nothing happened?`): domanda retorica, blacklist. E qui sarebbe anche falsa, perché la risposta la sappiamo già.

**Variante B:** `The button needs an email app. If nothing opened, copy the text below and send it to events@tiranatodo.com` (`108/140`). Mette la causa prima dell'effetto. Consegnata come seconda perché parte da un fatto tecnico invece che da quello che il lettore ha appena visto succedere, cioè niente.

⚠️ **Il testo che si copia in questa via di riserva è il blocco del lotto 3**, con `template.note.1` e `template.note.2` in testa. Restano validi e non li ho riscritti.

---

## 7 · `submit.next` — cosa succede dopo

```
A person reads every message. This is not automatic.
If something is missing we write back and ask for it.
If everything is there, your event goes on the site.
```

`52` + `53` + `52 car`, nessun tetto assegnato — tre righe, tre stati possibili, e nessuno resta scoperto.
La prima riga fa il lavoro che il brief chiede: dichiara che dall'altra parte c'è qualcuno. `This is not automatic` è scritto come rassicurazione ma è anche un'informazione operativa, perché spiega da sola perché la risposta non arriva in un minuto — ed è il modo di dire «ci vuole un po'» **senza dichiarare un tempo che non abbiamo**.
Le altre due coprono i due esiti. Chi ha mandato sa cosa aspettarsi in entrambi i casi, che è esattamente ciò che una durata gli darebbe.

⛔ **Zero tempi, e non è una dimenticanza.** `PLAN.md` §5 e `sop_invio_eventi_via_email.md` §3 contengono un «entro 24 ore» marcato `⏳ STIMA DA VALIDARE` dentro una SOP in stato `PROPOSTA`. Una promessa in pagina è opponibile quanto una in contratto e non si scrive su una stima. Resta aperto dal lotto 1, punto 1.

⚠️ **`your event goes on the site` è la stessa promessa già segnalata** in `Aperti (lotto 3)` punto 3: oggi il mockup non ha database. La stringa non cambia perché il problema non è di copy, ⛔ ma questa riga **non va in produzione** finché la pagina di invio pubblica un evento che non ha dove finire.

---

## Dichiarazione gate — lotto 4

| Gate | Esito |
|---|---|
| **Fatti** | 7 claim, tutti classificati. `24 hour clock` **VERIFICATO** (SOP §2.2 righe 4 e 6, `HH:MM` 24 ore). `1500 characters` **VERIFICATO** (SOP §2.2 riga 13 + `PLAN.md` §5.3). `JPG, PNG or WebP` e `up to 5 MB` **VERIFICATI** (SOP §2.2 foto + `PLAN.md` §5.4), stesso numero già in `submit.step3.text` e `rule.photo`: la pagina non dichiara due limiti. `Lek` come sola valuta **VERIFICATO** (SOP §2.2 riga 11 + §2.2 nota Prezzo). `Empty means English` **VERIFICATO** (SOP §2.2 riga 18 + `PLAN.md` §6). `CONSENT` bloccante **VERIFICATO** (SOP §2.2 riga 19 + §4 motivo 7). `your event goes on the site` **CORRETTO in stato di rischio**: vero per il sito futuro, falso oggi, segnalato sopra e in `Aperti (lotto 3)` punto 3. ⛔ Zero tempi di revisione, zero numeri di pubblico, zero prove sociali: non esistono e non sono stati inventati. ⚠️ `Rruga Sami Frashëri, Blloku` non è un claim: è la forma di un indirizzo dentro un esempio, e non afferma che lì ci sia un evento. |
| **Struttura** | Non applicabile nella forma standard: 55 stringhe di interfaccia (2 di testata, 19 etichette, 17 aiuti, 12 categorie, 4 bottoni, 7 errori, 2 chiusure, 3 righe di `submit.next`) dentro un percorso a passi. Nessun blocco piatto di 3+ paragrafi. Il pezzo esce già vestito: un campo per schermata, un'etichetta, un aiuto, due bottoni. |
| **Readability (EN)** | Stimata a mano, non da tool, sul testo definitivo di questo lotto. 48 frasi, 331 parole, **6,9 parole per frase**, ~1,30 sillabe per parola. **Flesch Reading Ease ≈ 88 (Easy) · Flesch-Kincaid Grade ≈ 3.** Target: organizzatore non madrelingua, B1-B2. Superato con margine. Le uniche parole fuori dalle 3.000 più frequenti sono nomi di formato (`JPG`, `PNG`, `WebP`, `Lek`) e non si sostituiscono. ⚠️ GULPEASE non applicabile: testo inglese. |
| **Offerta di vendita, 8 check** | **Non applicabile, e motivato.** Non è un pezzo che vende: nessun prezzo, nessuna offerta, nessuna azione da comprare. L'organizzatore ci sta facendo un favore. Il brief vieta esplicitamente registro promozionale. Applicare gli otto qui produrrebbe urgenza fabbricata su un favore. ⚡ **Il gate 3 (mostra che è facile) è però l'unico che governa davvero questo lotto, e non è dichiarato: è eseguito.** L'attrito si è **ridotto**, non dichiarato leggero: 19 righe da copiare a mano diventano 19 domande con una risposta sola, e nessuna stringa contiene la parola `easy`, `simple` o `quick`. |
| **Punteggio gate offerta** | Non calcolato, per la ragione sopra. Il punteggio della pagina resta quello del lotto 1: **86/120, soglia 75 superata**. ⚠️ Le voci **Prova 9** e **Facilità** andranno ricalcolate quando la pagina nuova è in piedi: la prova per dimostrazione del lotto 1 era il modello di mail visibile prima di scrivere, e nel questionario quel modello non si vede più fino all'ultimo passo. Non lo ricalcolo su una pagina che non esiste ancora. |
| **Lingua italiana (10 voci)** | Non applicabile al deliverable (copy inglese). Passata sulle note italiane di questa sezione: **10 voci controllate, 1 correzione** (concordanza in «le tre righe formano una promessa completa»). |
| **Blacklist AI** | Passata dedicata, pattern per pattern, sulle 55 stringhe nella forma definitiva: **zero match**. Verificato uno per uno: zero em dash · zero punti esclamativi (vincolo esplicito del brief, ricontrollato carattere per carattere) · zero antitesi «X, not Y» (controllato in particolare `Only the number in Lek`, riscritto proprio per togliere un `no euro` che ci somigliava) · zero domande retoriche (`Nothing happened?` scartato per questo) · zero tricolon retorici (`JPG, PNG or WebP` è un elenco di fatti) · zero `simply` / `easily` / `just` / `make sure to` (`Just answer and press send` scartato) · zero superlativi · zero aperture da AI · zero disclaimer difensivi · zero dichiarazioni di facilità. |
| **Deliverability** | Non applicabile: nessuna email inviata da noi. Il messaggio lo apre, lo firma e lo manda il lettore dal suo client. |
| **Budget caratteri** | **55 stringhe su 55 dentro il tetto, zero sfori, lista di sforo vuota e dichiarata.** Testata: 2/2 (18/45 e 69/110). Etichette: 19/19, la più larga `Your name or your group` **23/35**. Aiuti: 17/17, il più largo `ADDRESS` **76/85**, poi `PHOTO` 74/85 e `DESCRIPTION` 69/85. Categorie: 12/12, la più larga `Food & Drink` **12/35**. Bottoni: 4/4, il più largo **13/22**. Errori: 7/7, il più largo **47/60**. Chiusure: 2/2 (**102/140** e **110/140**). ⚡ Nessun tetto è stato chiesto più largo: ci stanno tutte, e i margini restituiti sono reali, non ottenuti tagliando un fatto. |
| **Traduzione SQ / IT — stringhe a filo** | ⚠️ Vincolo onorato ora. Con crescita del 15-20%: **`ADDRESS` help 76/85 sfora** (→ 87-91), ⚡ ma solo la prima frase cresce, il nome della via resta identico: cresce a ~84 e passa per un carattere. **A rischio reale, in ordine:** `PHOTO` help 74/85 (→ 85-89, **sfora**) · `DESCRIPTION` help 69/85 (→ 79-83, passa a filo) · `TIME_START` help 64/85 (→ 74-77, passa) · `ALL_DAY` help 62/85 (→ 71-74, passa). Sui bottoni: `Open my email` 13/22 (→ `Hap emailin tim` 15, `Apri la mia email` 17, passano) · `Copy the text` 13/22 (→ `Copia il testo` 14, passa). Sulle chiusure: `submit.fallback` 110/140 cresce solo per ~89 caratteri, l'indirizzo mail non si traduce (→ ~126, passa). ⛔ Non ho tradotto niente: SQ e IT non sono in questo lotto. |

---

## Aperti (lotto 4)

**1. La dodicesima categoria non esiste nella SOP, e la SOP dice «11».**
`sop_invio_eventi_via_email.md` §2.2 riga 2 fissa `CATEGORY` come «una delle **11** chiavi di §2.1». Davide ne ha aggiunta una dodicesima oggi (`workshop`). Finché la SOP dice 11, il controllo di §4.3 rimbalza ogni evento con `CATEGORY: workshop` come categoria non riconosciuta, e `docs/eventi.js` non ha l'etichetta. Ho scritto l'etichetta perché era nel brief; ⛔ non ho toccato né la SOP né `eventi.js`.
`DA CHIEDERE A OPS: la categoria workshop entra nell'elenco di §2.1 e §2.2, portando le chiavi da 11 a 12? Finché la SOP dice 11, il controllo §4.3 rifiuta un evento che il questionario permette di scegliere.`

**2. `Food &amp; Drink` è HTML escaped nella fonte.**
In `docs/eventi.js` l'etichetta è `Food &amp; Drink`. È corretto lì, dove finisce dentro HTML. ⛔ Se il questionario stampa la stessa costante come testo, o la manda dentro il corpo di una mail, il lettore vede `Food &amp; Drink`. L'etichetta che consegno è `Food & Drink`: la forma escaped è una questione di come si stampa, non di copy.
`DA CHIEDERE A SENTINEL: le etichette di TE_CAT_LABEL vengono stampate come HTML anche nel questionario e nel corpo della mail generata? Se in uno dei due finiscono come testo, Food &amp; Drink si vede a schermo.`

**3. Il tetto di 1500 caratteri di DESCRIPTION va contato da qualcuno.**
L'aiuto dichiara `Up to 1500 characters`, che è il numero verificato di SOP §2.2 riga 13. In un blocco da copiare a mano il tetto era una raccomandazione; ⛔ in un questionario diventa un comportamento: o il campo conta e blocca, o il numero scritto è una promessa che nessuno mantiene e la mail parte lunga. Serve anche una stringa per il contatore, che non ho scritto perché non so se esiste.
`DA CHIEDERE A SENTINEL: il campo DESCRIPTION del questionario ha un contatore e blocca a 1500 caratteri, o accetta di più? Se blocca serve la stringa del contatore, se non blocca l'aiuto dichiara un limite che non esiste.`

**4. L'indicatore di passo non è nel brief.**
Diciannove domande sono tante e il lettore che non sa quante ne mancano abbandona a metà. Un `Step 4 of 19` è una stringa da 12 caratteri e cambia il tasso di abbandono, ⛔ ma se i passi si raggruppano (per esempio 5 schermate da 4 domande) il testo è diverso e il numero pure. Non lo invento su una struttura che non ho visto.
`DA CHIEDERE A LORI: il questionario mostra 19 schermate da una domanda o raggruppa i campi in meno passi? Mi serve saperlo per scrivere l'indicatore di passo e per capire se «Next» va bene anche a fine gruppo.`

**5. Restano aperti dai lotti 1-3 e valgono ancora.**
Il tempo di revisione (lotto 1 punto 1, `DA CHIEDERE A OPS`) · `NEW` contro `NUOVO` nell'oggetto (lotto 1 punto 2, ⚡ ora **più urgente**: nel questionario l'oggetto lo compone la macchina, quindi la parola la scegliamo noi e il lettore non la vede più) · `the event goes on the site` senza database (lotto 3 punto 3, `DA CHIEDERE A DAVIDE`) · il profilo target simulato mancante (lotto 3 punto 6, `DA CHIEDERE A CHRISTOPHER`).

---
---

# Lotto 5 — le 13 stringhe che mancavano al questionario, 2026-09-05

**Mercato: EN internazionale · Lingua: EN · Set caricato: `skill_copy_mercati_eu` (sezione «EN internazionale») + `skill_copy_moderno_2026` + `regole_globali` + `blacklist_formule_ai`**

⚡ **Questo lotto completa il lotto 4, non lo sostituisce.** Le 55 stringhe del lotto 4 restano valide, ⚠️ salvo **quattro aiuti** che questa struttura a sei passi rende ridondanti o falsi: sono riscritti in §9, uno per uno, col motivo.
**Struttura ricevuta da LORI:** 6 passi di domande + 1 schermata finale. Nei passi 2 e 4 la domanda sì/no viene prima e comanda il campo dopo.
**Stesso lettore, stesso registro del lotto 4:** organizzatore a Tirana, spesso albanese, inglese B1-B2. Verbi finiti, una frase un fatto, zero idiomi, zero punti esclamativi.

---

## 1 · I titoli dei 6 passi

Tetto `26`. **Forma scelta: sintagma nominale, mai imperativo.** Un titolo grande in cima allo schermo che dice `Write your event` è un ordine, e il lettore ha già davanti i campi che glielo dicono. Il titolo serve a un'altra cosa: dargli in una parola l'argomento del passo, così sa se la cosa che gli manca (l'indirizzo, il prezzo) arriva più avanti o l'ha già saltata.

| passo | campi | titolo | car |
|---|---|---|---|
| 1 | TITLE, CATEGORY | `The event` | `9/26` |
| 2 | ALL_DAY, DATE_*, TIME_* | `When it happens` | `15/26` |
| 3 | VENUE_NAME, ADDRESS | `Where it happens` | `16/26` |
| 4 | IS_FREE, PRICE, TICKET_URL | `Tickets` | `7/26` |
| 5 | DESCRIPTION, PHOTO, LANGUAGE | `Details` | `7/26` |
| 6 | ORGANIZER_*, CONSENT | `About you` | `9/26` |

**Le scelte che non si vedono dalla tabella:**

- **`When it happens` / `Where it happens` in coppia.** `When` e `Where` da soli sarebbero 4 e 5 caratteri e si capirebbero, ⛔ ma due parole isolate in cima a uno schermo si leggono come un'etichetta di database, non come una domanda rivolta a te. Il verbo `happens` costa dieci caratteri e cambia chi sta parlando. È anche la parola già usata nel lotto 4 (`What happens`, `where it happens` in `VENUE_NAME`): il lettore la incontra tre volte e la impara.
- **`Tickets` e non `Price`.** Il passo comincia da `Is it free?`, quindi il titolo deve coprire anche l'evento gratuito: `Price` sopra un evento gratis è una contraddizione visibile. Variante: `Entry and price` (`15/26`), scartata perché `entry` è la parola meno frequente di tutto il set.
- **`About you` e non `Who sends it`.** Il passo 6 chiede nome, mail, telefono e consenso: è la parte dove uno si insospettisce di più. `About you` è la formula che qualunque modulo al mondo usa in quel punto, e la familiarità qui vale più dell'originalità. Variante: `Who sends it` (`12/26`).
- **`The event` al passo 1, ed è l'unico titolo generico.** È voluto: al primo schermo il lettore non sa ancora che sta rispondendo a domande divise per argomento, e un titolo stretto (`Name and type`, `13/26`) descrive due campi invece di aprire il percorso. ⚠️ Se LORI vuole simmetria piena con gli altri cinque, `Name and type` è pronto e passa il tetto.

⚠️ **Traduzione:** il più lungo è `Where it happens` (16). IT `Dove si svolge` (14), SQ `Ku zhvillohet` (13): crescono meno dell'inglese perché il pronome sparisce. **Nessuno dei sei è a filo.**

---

## 2 · Il titolo della schermata finale

```
Your message is ready
```

`21/30 car` — dice lo stato, non l'azione, perché l'azione ce l'ha già scritta sul bottone rosso sotto. `message` e non `email`: in questa pagina le due parole fanno due lavori diversi e il lotto 4 li aveva già separati in `submit.ready` (**il messaggio** è il testo che abbiamo scritto noi, **l'email app** è il programma suo). Tenere la distinzione su tutte le stringhe della schermata è ciò che gli fa capire che non stiamo mandando niente al posto suo.

**Le altre due varianti:** `The last step` (`13/30`) · `Your email is ready` (`19/30`).
⛔ `Your email is ready` è la più immediata **e la scarto**: rompe la distinzione qui sopra, e un lettore che legge «la tua email è pronta» può concludere che è già partita.

⚡ **Una conseguenza sul lotto 4, e va decisa insieme al titolo.** `submit.ready` comincia con `Your answers are ready.` Con questo titolo sopra, il lettore legge due volte `is/are ready` in tre centimetri. Se il titolo passa, `submit.ready` si accorcia così:

```
The button opens your email app with the message written. You only press send.
```

`77/140 car` — perde la prima frase, che ora la dice il titolo, e tiene i due fatti che il titolo non dice. ⚠️ Se invece LORI sceglie `The last step` come titolo, `submit.ready` **resta quello del lotto 4** e non si tocca.

---

## 3 · L'indicatore di avanzamento

```
Step {n} of {total}
```

Tetto `16`, **modello 19 caratteri con i segnaposto, `11/16` a schermo** nel caso più lungo (`Step 6 of 6`). ⚡ Il conteggio che conta è quello renderizzato, e i due segnaposto sono cifre singole.

**Confermo questa forma e non ne propongo una diversa**, per una ragione sola: `Step` è la parola che dice al lettore che la cosa **finisce**, ed è l'unico dato che gli manca quando decide se cominciare. Le due varianti più corte le do lo stesso: `{n} of {total}` (`6/16` a schermo) e `{n}/{total}` (`3/16`), ⛔ ma un `3 / 6` isolato in cima allo schermo si legge come un punteggio o come un conteggio di errori.

⚠️ **Il totale è `6`, non `7`, e la schermata finale non porta indicatore.** La settima non è un passo: non chiede niente, non ha `Next`, ed è il premio. Uno `Step 7 of 7` su quella schermata riaprirebbe la domanda «devo fare un'altra cosa?» nel punto esatto in cui gli stiamo dicendo che ha finito.

⚠️ **Traduzione:** IT `Passo 6 di 6` (12), SQ `Hapi 6 nga 6` (12). Entrambi dentro 16, **a filo di 4 caratteri**. ⛔ Se il contenitore stringe sotto i 12 la forma va cambiata in tutte e tre le lingue insieme, non solo dove sfora.

---

## 4 · `Yes` e `No`

| stringa | testo | car |
|---|---|---|
| `choice.yes` | `Yes` | `3/6` |
| `choice.no` | `No` | `2/6` |

**Confermati, e non c'è una versione migliore.** Sono le due parole più frequenti dell'inglese, si riconoscono senza leggerle e non hanno sinonimo più corto. ⛔ Scartati `Yes, it does` / `No, it does not`: rispondono alla domanda per esteso, sforano il tetto, e obbligano a scrivere una coppia diversa per ogni campo.

⚠️ **Valgono per due campi, non per tre.** `ALL_DAY` e `IS_FREE` sono coppie sì/no. **`CONSENT` è una casella**, come fissa `PLAN.md` §5.5 (checkbox **non pre-spuntata**), e la sua stringa è §9 qui sotto.
⛔ Se LORI decidesse di rendere anche il consenso come coppia `Yes`/`No`, **nessuno dei due può essere preselezionato**: una coppia con `Yes` già acceso è la casella pre-spuntata scritta in un altro modo, e la SOP §4 motivo 7 la rifiuta a valle.

⚠️ **Traduzione:** IT `Sì`/`No`, SQ `Po`/`Jo`. Nessun rischio.

---

## 5 · Le tre scelte del campo foto

Tetto `12` l'una. Valori tecnici sottostanti: `ATTACHED`, `LINK:<url>`, `NONE` (SOP §2.2 riga 14).

| valore | etichetta | car |
|---|---|---|
| `ATTACHED` | `A file` | `6/12` |
| `LINK:<url>` | `A link` | `6/12` |
| `NONE` | `No photo` | `8/12` |

**Tre nomi, stessa forma, e il verbo sta fuori.** L'etichetta del campo è `Photo` (lotto 4, campo 14), quindi le tre scelte rispondono a «Photo: …» e si leggono come una frase sola. Con le etichette verbali (`Attach file` / `Paste a link`) le tre voci diventano tre ordini diversi, e il lettore deve decidere quale sta eseguendo invece di scegliere cosa ha.

⚡ **`A file` e non `Attached`.** `Attached` descrive uno stato che al momento della scelta **è falso**: il file non è ancora allegato, e non lo sarà finché non apre la mail. È il punto che rende necessario il promemoria di §13, e un'etichetta che dà per fatta una cosa da fare peggiora quel problema.

**Le varianti:** `Attach file` (`11/12`) · `Paste a link` (`12/12`) · `None` (`4/12`). ⛔ `None` da solo lo scarto anche come variante: in un elenco di tre voci si legge come «nessuna delle precedenti», che è un'altra cosa.

⚠️ **Traduzione:** IT `Un file` (7) / `Un link` (7) / `Nessuna foto` (**12/12, a filo esatto**). SQ `Një skedar` (10) / `Një lidhje` (10) / `Pa foto` (7). ⚡ L'italiano non ha un carattere di margine: se il tetto 12 è misurato su un chip reale, `Nessuna foto` ci sta solo se il contenitore conta 12 esatti. Alternativa italiana pronta: `Senza foto` (10).

---

## 6 · I tre nomi di lingua

Tetto `12` l'uno. Valori: `EN`, `SQ`, `IT` (SOP §2.2 riga 18).

| valore | etichetta | car |
|---|---|---|
| `EN` | `English` | `7/12` |
| `SQ` | `Albanian` | `8/12` |
| `IT` | `Italian` | `7/12` |

**Nomi in inglese, non endonimi.** Nell'interfaccia inglese `Shqip` e `Italiano` costringono chi non conosce quelle lingue a indovinare, ⚡ e chi sceglie questo campo spesso sta dichiarando una lingua che **non** parla (un evento in albanese descritto da un organizzatore che compila in inglese). ⚠️ Nell'interfaccia SQ diventano `Anglisht` / `Shqip` / `Italisht`, in quella IT `Inglese` / `Albanese` / `Italiano`: **endonimo solo per la lingua dell'interfaccia**, che è la convenzione che tutti riconoscono.

⛔ Scartati `EN` / `SQ` / `IT` come etichette a schermo: sono i valori del formato mail, non parole. `SQ` non si deduce da `Shqip` se non sai già cosa vuol dire.

⚠️ **Il campo è facoltativo e vuoto vale `English`** (SOP §2.2 riga 18, e l'aiuto del lotto 4 lo dice). Tre voci a schermo però invitano a sceglierne una, e se una è preselezionata il campo non è più vuoto. Vedi `Aperti (lotto 5)` punto 2.

---

## 7 · Il campo dove si incolla il link della foto

Compare solo se ha scelto `A link`.

### photo.link.label

```
Link to the photo
```

`17/35 car` — ripete la parola `link` che ha appena scelto, così il campo nuovo che compare si aggancia da solo alla scelta di un secondo prima. ⛔ Niente `URL`: è la parola che chi non lavora nel web salta.

### photo.link.help

```
Paste the full link of the file. It must open without a password.
```

`65/95 car` — due frasi, due errori diversi. La prima è il link tagliato o scritto a mano; **la seconda è quella che fa il lavoro**: il caso frequente è un file su Drive condiviso solo con chi ha l'account, e da fuori quel link non si apre. Detto dopo, quando la foto manca, costa una mail di andata e ritorno.
⛔ Non scrivo «controlla i permessi di condivisione»: descrive un menu che cambia da servizio a servizio. `It must open without a password` descrive il risultato, che è verificabile da chiunque.

**Variante:** `Paste the full link, from Drive, WeTransfer or similar. It must open without a password.` (`87/95`). Nomina i due servizi che la SOP §2.2 già cita, ⚠️ **e non la consegno come prima**: in SQ e IT la parte traducibile cresce e il totale arriva a ~104, che sfora il tetto nuovo. La versione da 65 cresce a ~77.

---

## 8 · Il marcatore «facoltativo»

```
Optional
```

`8/10 car` — una parola, accanto all'etichetta, sugli **otto** campi non obbligatori (`DATE_END`, `TIME_END`, `TIME_START` quando è giorno intero, `TICKET_URL`, `DESCRIPTION`, `PHOTO`, `ORGANIZER_PHONE`, `LANGUAGE`).

⛔ Scartato `Not required` (`12/10`, sfora) e `If you have it` (`14/10`, sfora): dicono la stessa cosa in più caratteri.

⚠️ **Traduzione, ed è l'unica riga di questo lotto dove l'italiano sfora davvero.** IT `Facoltativo` = **11/10**. Va usato `Opzionale` (9), che è la parola dei moduli e sta nel tetto. SQ `Opsionale` (9), passa.

### ⚡ Il marcatore rende ridondanti due aiuti del lotto 4, e li riscrivo

Il PM ha chiesto di guardare quali. Sono due, e sono quelli che spendono caratteri per dare un **permesso** che ora dà il marcatore:

| campo | aiuto lotto 4 | car | aiuto lotto 5 | car |
|---|---|---|---|---|
| 6 TIME_END | `You can leave this empty if you do not know it.` | `47/85` | `Only if you know it.` | `20/85` |
| 13 DESCRIPTION | `Optional, but the page looks empty without it. Up to 1500 characters.` | `69/85` | `The page looks empty without it. Up to 1500 characters.` | `55/85` |

- **TIME_END.** L'intera frase era il permesso. Tolto il permesso resta la condizione, ed è quella che serve: `Only if you know it` ha la stessa forma di `DATE_END` (`Only if the event ends on a different day`), quindi i due campi vicini si leggono in parallelo.
- **DESCRIPTION.** Cade solo la parola `Optional,`, che ora sta accanto all'etichetta. ⚡ **Il resto non si tocca**: `but the page looks empty without it` non è un permesso, è la conseguenza, ed è l'unica cosa che convince a scrivere la descrizione.

⛔ **Non sono ridondanti e restano come sono** gli altri sei: `DATE_END` (condizione, non permesso) · `TICKET_URL` (condizione) · `ORGANIZER_PHONE` (dice a cosa serve e che resta privato) · `LANGUAGE` (dichiara il default, cosa che `Optional` non dice) · `PHOTO` (riscritto in §9 per un altro motivo) · `TIME_START` (riscritto in §9).

---

## 9 · Il testo del consenso

```
Yes, you can publish this event on the site, with the name I wrote above.
```

`73/130 car` — casella **mai pre-spuntata** (`PLAN.md` §5.5).

- **Comincia con `Yes`** perché è la parola che spuntando la casella lui sta dicendo. Il testo accanto a una checkbox si legge come la propria dichiarazione, non come una nostra clausola, e in prima persona la frase ha un soggetto solo.
- **`this event` e non `my data`.** Autorizza la pubblicazione della cosa che ci ha appena descritto, che è esattamente quello che facciamo.
- **`with the name I wrote above`** chiude il solo pezzo che potrebbe sorprenderlo: che il nome dell'organizzatore compare in pagina. Il lotto 4 lo dice già nell'aiuto di `ORGANIZER_NAME` (`We show this name with the event`), qui diventa la cosa che autorizza.
- ⛔ **Nessuna promessa sulla sicurezza dei dati**, come vincolato dal brief e come già dichiarato nel lotto 4: `PLAN.md` §6 fase 6 mette la pagina privacy sotto LEX e **non è scritta**. Una riga tipo «i tuoi dati sono al sicuro» sarebbe una promessa senza documento che la regge.
- ⛔ **Nessun link a una privacy policy**, per la stessa ragione: il link non esiste. Vedi `Aperti (lotto 5)` punto 3.

**Le altre due varianti:** `Yes, you can publish this event on TiranaToDo.` (`46/130`), più corta e non nomina il nome pubblico · `Yes, you can publish this event and the photo I send, with the name I wrote above.` (`81/130`), che copre anche l'immagine. ⚠️ La terza è la più completa **e la tengo seconda**: nomina la foto anche quando ha scelto `No photo`, e un consenso che parla di una cosa che non hai mandato fa fermare a rileggere.

⚠️ **Traduzione:** 73 → ~84-88. Passa con margine.

---

## 10 · Il contatore dei caratteri

```
{n} / 1500
```

Tetto `20`, **modello 10 caratteri, `11/20` a schermo** nel caso più lungo (`1500 / 1500`).

Nessuna parola, e non è pigrizia: il contatore sta sotto un campo di testo che il lettore sta usando, quindi si guarda con la coda dell'occhio mentre scrive. `1240 of 1500 characters` (`23/20`) sfora, e in SQ e IT sforerebbe anche a tetto 20.

**Variante:** `{n} of 1500` (`11/20` a schermo), se `/` sembra troppo tecnico. ⛔ Scartato `1500 characters left`: conta alla rovescia, e un numero che scende mentre scrivi somiglia a un tempo che finisce.

⚡ **Il numero 1500 è VERIFICATO** (SOP §2.2 riga 13 + `PLAN.md` §5.3, che prevede il contatore visibile). ⚠️ `PLAN.md` dice **visibile**, non dice **bloccante**: resta aperto dal lotto 4 punto 3 e sta in `Aperti (lotto 5)` punto 4.

---

## 11 · L'errore quando il consenso non è spuntato

```
Please tick the box, or we cannot publish it.
```

`45/60 car` — ottava stringa della serie del lotto 4, stessa apertura `Please`, stessa regola: dice cosa fare adesso, non cosa è andato storto.
La seconda metà esiste perché senza di lei la frase è un ordine senza motivo. ⚡ È la stessa costruzione dell'aiuto di `CONSENT` (`We need a yes. Without it we cannot publish your event.`): dice **cosa non possiamo fare noi**, che è l'unica leva onesta quando il blocco è nostro.

**Variante:** `Please tick the box, or we cannot publish your event.` (`53/60`). ⚠️ Più chiara di un carattere di senso e più fragile in traduzione: in SQ e IT arriva a 61-64 e **sfora**. La versione da 45 cresce a ~52-54 e passa.

⛔ Scartato `You must accept to continue.` (`27/60`): `must` è l'unico imperativo duro di tutto il set, e cade sul campo dove il lettore è più libero di dire di no.

---

## 12 · L'etichetta sopra il blocco copiabile

```
Your message: read it or copy it.
```

`33/45 car` — **due lavori, due verbi, in ordine di frequenza d'uso.** `read it` viene prima perché quasi tutti quel blocco lo rileggono e quasi nessuno lo copia: la via di riserva serve solo a chi non ha un client di posta.

- **`Your message`** riprende la parola del titolo (§2) e della `submit.ready`: è lo stesso oggetto nominato allo stesso modo tre volte, e a un B1 questa ripetizione toglie un dubbio invece di annoiare.
- **I due punti** dividono l'oggetto dalle due azioni possibili senza costruire una subordinata.
- ⛔ Non dice `if the button did not work`: quella diagnosi è già in `submit.fallback` (lotto 4 §6b), che sta accanto. Ripeterla nell'etichetta trasformerebbe il blocco in «la cosa per quando si rompe», e chi vuole solo rileggere non lo aprirebbe.

**Le altre due varianti:** `Check your answers, or copy the text.` (`36/45`) · `Your message, ready to read or to copy.` (`39/45`). ⚠️ La terza è la più elegante e in SQ e IT arriva a ~46, quindi **sfora**. La consegnata cresce a ~39-40.

---

## 13 · Il promemoria della foto allegata

Compare nella schermata finale solo se ha scelto `A file`.

```
Add the photo file to the email before you send it. The site cannot attach it for you.
```

`86/110 car` — due frasi, e la seconda è quella che rende la prima credibile.

- **Prima l'azione, poi il motivo.** Un promemoria che comincia con la spiegazione (`The site cannot…`) si legge come una scusa nostra, e chi legge una scusa smette prima dell'istruzione.
- **`The site cannot attach it for you` è un limite dichiarato, non un difetto.** È il fatto tecnico vero: la pagina compone il testo, il file sta sul suo dispositivo e la mail la manda lui. Detto così, chi lo legge capisce **perché** deve ricordarsene, e un motivo compreso si ricorda meglio di un avviso.
- ⛔ Niente `Do not forget`: è la formula che chiude ogni promemoria automatico del mondo, e si salta insieme al resto.

**Variante:** `You chose to attach the photo. Remember to add the file to the email before you send it.` (`88/110`). Ricollega alla sua scelta, ⚠️ ma spende 30 caratteri per dirgli una cosa che sa e non spiega perché il sito non lo fa da solo. In SQ e IT arriva a ~101-106: passa a filo.

---

## 14 · Il bottone dell'ultimo passo

```
See the message
```

`15/22 car` — sostituisce `Next` **solo al passo 6**. Sugli altri cinque `Next` resta.

`Next` promette un'altra domanda, e dopo il passo 6 non ce ne sono: chi legge `Next` sul consenso non sa di essere all'ultima e continua a spendere l'attenzione a razione. `See the message` dice due cose in tre parole: **le domande sono finite**, e **la prossima schermata contiene una cosa da guardare**, che è precisamente ciò che succede.

- **`the message` e non `my email`** per la distinzione già dichiarata in §2: `Open my email` sulla schermata dopo apre il **programma**, questo bottone mostra il **testo**. Due bottoni che dicono `email` uno dopo l'altro fanno pensare che il primo abbia già fatto partire qualcosa.
- **Le altre due varianti:** `Prepare my email` (`16/22`) · `Write it for me` (`15/22`). La seconda dice il valore vero (il messaggio lo scriviamo noi) ed è la più calda del gruppo. ⚠️ La lascio seconda perché `for me` su un bottone che il lettore preme può leggersi come «fallo tu al posto mio», e mandare non lo facciamo noi.
- ⛔ Scartato `Finish` (`6/22`): dice che è finito ma non dice cosa vede dopo, e su un percorso che continua per una schermata è una piccola bugia.

⚠️ **Traduzione:** IT `Vedi il messaggio` (17), SQ `Shiko mesazhin` (14). Dentro 22, **il più largo del set bottoni**.

---

## 15 · Le altre due riscritture del lotto 4, e il tetto allargato

Il PM ha allargato il tetto dell'aiuto `PHOTO` da 85 a **95**. Ne approfitto per una ragione diversa dallo spazio: con le tre scelte di §5 a schermo, l'aiuto del lotto 4 nominava le opzioni **con parole diverse** da quelle dei chip.

| campo | aiuto lotto 4 | car | aiuto lotto 5 | car |
|---|---|---|---|---|
| 14 PHOTO | `Attach a file, paste a link, or choose none. JPG, PNG or WebP, up to 5 MB.` | `74/85` | `Choose a file, a link, or no photo. JPG, PNG or WebP, up to 5 MB.` | `65/95` |
| 4 TIME_START | `Use the 24 hour clock, like 21:00. Skip it for an all day event.` | `64/85` | `Use the 24 hour clock, like 21:00.` | `34/85` |

- **PHOTO.** Le tre parole dell'aiuto ora sono **le stesse tre dei chip**, alla lettera. ⚡ Con 65 caratteri il tetto nuovo di 95 non serve nemmeno: in SQ e IT cresce a ~75-78, quindi **sarebbe rientrato anche negli 85 vecchi**. Il tetto allargato resta comunque utile come margine, e lo dichiaro non usato.
- **TIME_START, ed è una correzione di sostanza, non di stile.** Nel lotto 4 `ALL_DAY` veniva **dopo** le ore, quindi `Skip it for an all day event` era l'unico posto dove il lettore imparava che poteva saltarle. Con la struttura di LORI la domanda sì/no viene **prima**, quindi a quel punto ha già risposto. ⚠️ La frase resta necessaria **solo se** i campi ora non spariscono: vedi `Aperti (lotto 5)` punto 1, dove sta anche la versione da rimettere.

---

## Dichiarazione gate — lotto 5

| Gate | Esito |
|---|---|
| **Fatti** | 4 claim, tutti classificati. `1500` nel contatore **VERIFICATO** (SOP §2.2 riga 13 + `PLAN.md` §5.3 «contatore visibile»). `JPG, PNG or WebP, up to 5 MB` nell'aiuto PHOTO riscritto **VERIFICATO** (SOP §2.2 nota Foto + `PLAN.md` §5.4), stesso numero già in pagina. Casella di consenso **non pre-spuntata VERIFICATO** (`PLAN.md` §5.5 + SOP §4.3 nota su `CONSENT`). `EN` come default a campo vuoto **VERIFICATO** (SOP §2.2 riga 18 + `PLAN.md` §6). ⛔ Zero tempi, zero numeri di pubblico, zero promesse sui dati: la pagina privacy non esiste (`PLAN.md` fase 6, owner LEX) e nessuna stringa ne presuppone una. ⚠️ `The site cannot attach it for you` è una descrizione del nostro meccanismo, non un claim. |
| **Struttura** | Non applicabile nella forma standard: 27 stringhe di interfaccia (6 titoli di passo, 1 titolo finale, 1 indicatore, 2 sì/no, 3 scelte foto, 3 lingue, 1 etichetta + 1 aiuto link foto, 1 marcatore, 1 consenso, 1 contatore, 1 errore, 1 etichetta blocco, 1 promemoria, 1 bottone) più 4 riscritture di aiuti del lotto 4. Nessun blocco piatto di 3+ paragrafi. |
| **Readability (EN)** | Stimata a mano, non da tool, sul testo definitivo. 21 frasi, 148 parole, **7,0 parole per frase**, ~1,28 sillabe per parola. **Flesch Reading Ease ≈ 89 (Easy) · Flesch-Kincaid Grade ≈ 3.** Target B1-B2 non madrelingua: superato con margine. Parole fuori dalle 3.000 più frequenti: solo i nomi di formato (`JPG`, `PNG`, `WebP`) e i tre nomi di lingua. ⚠️ GULPEASE non applicabile: testo inglese. |
| **Offerta di vendita, 8 check** | **Non applicabile, e motivato come nel lotto 4.** Nessun prezzo, nessuna offerta, nessuna azione da comprare: l'organizzatore ci sta facendo un favore. ⚡ Il gate 3 (mostra che è facile) è di nuovo l'unico che governa, ed è **eseguito, non dichiarato**: l'indicatore di passo dice che finisce, il marcatore `Optional` toglie otto obblighi percepiti, il bottone finale dice cosa succede prima che succeda. ⛔ Nessuna stringa contiene `easy`, `simple`, `quick`, `fast`. |
| **Punteggio gate offerta** | Non calcolato, per la ragione sopra. Resta il **86/120 del lotto 1, soglia 75 superata**, con la stessa riserva già scritta al lotto 4: le voci **Prova 9** e **Facilità** si ricalcolano quando la pagina nuova è in piedi. |
| **Lingua italiana (10 voci)** | Non applicabile al deliverable (copy inglese). Passata sulle note italiane di questo lotto: **10 voci controllate, 2 correzioni** (accordo participio in «le tre scelte … si leggono», ripetizione di «che» in §13). |
| **Blacklist AI** | Passata dedicata, pattern per pattern, sulle 27 stringhe nella forma definitiva: **zero match**. Verificato uno per uno: zero em dash · zero punti esclamativi (ricontrollato carattere per carattere) · zero antitesi «X, not Y» · zero domande retoriche · zero `just` / `simply` / `easily` / `make sure to` / `don't forget` (`Do not forget` scartato in §13) · zero superlativi · zero aperture da AI · zero congratulazioni anticipate · zero urgenza. |
| **Deliverability** | Non applicabile: nessuna email inviata da noi. |
| **Budget caratteri** | **27 stringhe su 27 dentro il tetto, zero sfori, lista di sforo vuota e dichiarata.** Titoli passo: 6/6, il più largo `Where it happens` **16/26**. Titolo finale **21/30**. Indicatore **11/16** a schermo. Sì/no: 2/2, **3/6**. Scelte foto: 3/3, la più larga **8/12**. Lingue: 3/3, la più larga **8/12**. Link foto: **17/35** + **65/95**. Marcatore **8/10**. Consenso **73/130**. Contatore **11/20** a schermo. Errore **45/60**. Etichetta blocco **33/45**. Promemoria **86/110**. Bottone **15/22**. ⚡ Il tetto allargato di PHOTO (85→95) **non è stato usato**: l'aiuto riscritto sta in 65 e sarebbe rientrato anche nel vecchio. |
| **Traduzione SQ / IT — stringhe a filo** | ⚠️ Vincolo onorato ora, crescita stimata 15-20%. **Sfora una sola voce, ed è italiana: `Optional` → `Facoltativo` 11/10.** Soluzione dentro il tetto: `Opzionale` (9). **A filo, in ordine di rischio:** scelta foto `No photo` → IT `Nessuna foto` **12/12 esatti** (alternativa pronta `Senza foto`, 10) · indicatore `Step {n} of {total}` → IT `Passo 6 di 6` e SQ `Hapi 6 nga 6`, **12/16** · promemoria foto 86/110 → ~99-103, passa · bottone `See the message` → IT `Vedi il messaggio` 17/22, passa · aiuto link foto 65/95 → ~77, passa · consenso 73/130 → ~86, passa con margine. ⛔ Non ho tradotto niente: SQ e IT non sono in questo lotto, le cifre sopra sono stime di capienza. |

---

## Aperti (lotto 5)

**1. I campi condizionati spariscono o restano grigi?**
La domanda sì/no ora viene prima, e da questo dipendono **tre stringhe già scritte**. Se `TIME_START`/`TIME_END` spariscono quando `ALL_DAY` è `Yes`, allora l'aiuto `TIME_START` va accorciato a `Use the 24 hour clock, like 21:00.` (34/85, §15) **e l'ottava stringa d'errore del lotto 4** (`Please pick a time, or say it lasts all day.`) diventa irraggiungibile e si toglie. Se invece i campi restano a schermo, tutte e due le versioni del lotto 4 restano valide come sono. Stessa domanda su `PRICE` quando `IS_FREE` è `Yes`.
`DA CHIEDERE A LORI: nei passi 2 e 4, quando la risposta sì/no rende inutile il campo dopo, quel campo sparisce o resta visibile e disattivato? Da questo dipendono l'aiuto di TIME_START e l'errore condizionato del lotto 4.`

**2. `LANGUAGE` con tre voci a schermo perde il suo default.**
La SOP dice che il campo assente vale `EN`. Tre chip invitano a sceglierne uno, e se uno è preselezionato il campo non è più vuoto: il valore `EN` verrebbe scritto nella mail anche per un evento in albanese di chi non ha guardato quella riga.
`DA CHIEDERE A LORI: le tre voci di LANGUAGE nascono tutte spente, con la possibilità di andare avanti senza sceglierne nessuna? Se una è preselezionata, l'aiuto «Empty means English» non descrive più quello che succede.`

**3. Il consenso non ha un documento a cui rimandare.**
`PLAN.md` mette la pagina privacy in fase 6 con LEX come owner, e oggi non esiste. La stringa di consenso che consegno funziona senza link, ⛔ ma quando la pagina esiste il testo va rivisto: un consenso alla pubblicazione che non rimanda a nulla è accettabile in un mockup, non su un sito che raccoglie mail e telefoni di persone vere.
`DA CHIEDERE A LEX: il consenso alla pubblicazione dell'evento regge senza una privacy policy pubblicata, dato che il modulo raccoglie email e telefono? Se serve un rimando, mi serve sapere a cosa rimanda prima di riscrivere i 73 caratteri.`

**4. Il contatore è visibile, ma nessuno ha detto se blocca.**
`PLAN.md` §5.3 prevede il contatore visibile, quindi il punto 3 degli `Aperti (lotto 4)` è **mezzo chiuso**: la stringa serve e l'ho scritta. ⛔ Resta aperta l'altra metà, che è la sola che cambia il copy: se il campo accetta più di 1500 caratteri, l'aiuto `Up to 1500 characters` dichiara un limite che non c'è.
`DA CHIEDERE A SENTINEL: il campo DESCRIPTION blocca a 1500 caratteri o accetta di più mostrando il contatore in rosso? Se accetta di più, l'aiuto va riscritto perché oggi promette un limite.`

**5. Restano aperti dai lotti 1-4 e valgono ancora.**
Il tempo di revisione (lotto 1 punto 1, `DA CHIEDERE A OPS`) · `NEW` contro `NUOVO` nell'oggetto (lotto 1 punto 2) · la dodicesima categoria `workshop` che la SOP non conosce (lotto 4 punto 1, `DA CHIEDERE A OPS`) · `Food &amp; Drink` HTML escaped (lotto 4 punto 2, `DA CHIEDERE A SENTINEL`) · `the event goes on the site` senza database (lotto 3 punto 3, `DA CHIEDERE A DAVIDE`) · il profilo target simulato mancante (lotto 3 punto 6, `DA CHIEDERE A CHRISTOPHER`).
